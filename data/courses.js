const courseData = {
  "modules": [
    {
      "id": "module1",
      "title": "Module 1: Introduction & Lab Setup",
      "sections": [
        {
          "id": "m1s1",
          "title": "1.1 Why PowerShell for SQL Server DBAs",
          "brief": "<h4>Why PowerShell for SQL Server DBAs</h4><ul><li>Automate repetitive DBA tasks — backups, restores, patching</li><li>Manage multiple SQL instances from a single script</li><li>DBATools — 500+ commands built specifically for SQL Server</li><li>Integrate with Windows Task Scheduler for unattended automation</li><li>Version control your DBA scripts with Git</li><li>Consistent, repeatable operations across all environments</li></ul>",
          "description": "<p><strong>PowerShell is the most powerful automation tool available to SQL Server DBAs.</strong> It provides native access to SQL Server Management Objects (SMO), integrates with Windows infrastructure, and has a thriving community module ecosystem — most notably DBATools.</p><p>As a DBA, you perform dozens of repetitive tasks daily: checking backups, managing logins, monitoring disk space, patching servers. PowerShell lets you automate all of these, freeing you to focus on architecture, performance tuning, and strategic work.</p><h4>Key Benefits</h4><ul><li><strong>DBATools module:</strong> 500+ commands purpose-built for SQL Server administration</li><li><strong>Multi-server management:</strong> Run the same command against 50 instances simultaneously</li><li><strong>Consistency:</strong> Scripts run identically every time — no missed steps</li><li><strong>Integration:</strong> Works with Active Directory, Windows services, file systems, and APIs</li></ul>",
          "syntax": "# Quick reference\nGet-DbaDatabase -SqlInstance server1\nBackup-DbaDatabase -SqlInstance server1 -Database mydb\nTest-DbaConnection -SqlInstance server1\nGet-DbaLastBackup -SqlInstance server1",
          "examples": [
            {"name": "Connect to SQL", "code": "# Connect and list databases\n$server = 'localhost\\SQL2019'\nGet-DbaDatabase -SqlInstance $server |\n    Format-Table Name, Status, Size, RecoveryModel\n\n# Test connectivity\nTest-DbaConnection -SqlInstance $server"},
            {"name": "Health Check", "code": "# Quick health check across multiple servers\n$servers = 'SQL-PROD-01', 'SQL-PROD-02', 'SQL-DR-01'\n\n# Test all connections\nTest-DbaConnection -SqlInstance $servers |\n    Format-Table SqlInstance, ConnectSuccess, DomainName\n\n# Check last backup times\nGet-DbaLastBackup -SqlInstance $servers |\n    Where-Object { $_.LastFullBackup -lt (Get-Date).AddDays(-1) } |\n    Select-Object SqlInstance, Database, LastFullBackup"},
            {"name": "Why Automate", "code": "# Compare manual vs automated DBA tasks\n$tasks = @(\n    [PSCustomObject]@{Task='Backup 20 databases'; Manual='45 min'; Automated='2 min'}\n    [PSCustomObject]@{Task='Check all backups'; Manual='30 min'; Automated='10 sec'}\n    [PSCustomObject]@{Task='Copy logins to DR'; Manual='60 min'; Automated='30 sec'}\n    [PSCustomObject]@{Task='Patch 10 servers'; Manual='4 hours'; Automated='20 min'}\n)\n\n$tasks | Format-Table -AutoSize\n\nWrite-Host \"`nPowerShell + DBATools = DBA Superpowers\" -ForegroundColor Cyan"}
          ]
        },
        {
          "id": "m1s2",
          "title": "1.2 Setting Up Your Lab Environment",
          "brief": "<h4>Setting Up Your Lab Environment</h4><ul><li>Windows Server 2019/2022 or Windows 10/11 Pro</li><li>SQL Server 2019 or 2022 Developer Edition (free)</li><li>PowerShell 5.1 (built-in) or PowerShell 7+</li><li>Run PowerShell as Administrator for SQL operations</li><li>Install DBATools module from PowerShell Gallery</li><li>Configure execution policy: Set-ExecutionPolicy RemoteSigned</li></ul>",
          "description": "<p><strong>A proper lab environment is essential for learning PowerShell SQL Server administration safely.</strong> You need a SQL Server instance you can experiment with freely — never practice on production systems.</p><h4>Requirements</h4><ul><li><strong>OS:</strong> Windows Server 2019/2022 or Windows 10/11 Pro</li><li><strong>SQL Server:</strong> Developer Edition 2019 or 2022 (free, full-featured)</li><li><strong>PowerShell:</strong> 5.1 (included with Windows) or PowerShell 7+</li><li><strong>Memory:</strong> 8GB minimum, 16GB recommended</li><li><strong>Disk:</strong> 50GB free space for databases and backups</li></ul><h4>Setup Steps</h4><ul><li>Install SQL Server Developer Edition with default instance</li><li>Enable SQL Server Agent service</li><li>Open PowerShell as Administrator</li><li>Set execution policy: <code>Set-ExecutionPolicy RemoteSigned</code></li><li>Install DBATools: <code>Install-Module dbatools</code></li></ul>",
          "syntax": "# Lab setup commands\nSet-ExecutionPolicy RemoteSigned -Force\nInstall-Module dbatools -Scope CurrentUser\nImport-Module dbatools\n\n# Verify SQL Server is running\nGet-Service *SQL* | Format-Table Name, Status\n\n# Test connection to your lab instance\nTest-DbaConnection -SqlInstance localhost",
          "examples": [
            {"name": "Check Prerequisites", "code": "# Verify your lab environment\nWrite-Host '=== Lab Environment Check ===' -ForegroundColor Cyan\n\n# PowerShell version\nWrite-Host \"`nPowerShell Version:\" $PSVersionTable.PSVersion\n\n# Check if SQL Server services are running\nGet-Service *SQL* |\n    Format-Table Name, Status, StartType -AutoSize\n\n# Check execution policy\nWrite-Host \"`nExecution Policy:\" (Get-ExecutionPolicy)\n\n# Check if dbatools is installed\nif (Get-Module -ListAvailable -Name dbatools) {\n    Write-Host 'dbatools: INSTALLED' -ForegroundColor Green\n} else {\n    Write-Host 'dbatools: NOT INSTALLED' -ForegroundColor Red\n    Write-Host 'Run: Install-Module dbatools -Scope CurrentUser'\n}"},
            {"name": "Install DBATools", "code": "# Install DBATools module\n# Run PowerShell as Administrator\n\n# Set execution policy (required once)\nSet-ExecutionPolicy RemoteSigned -Force -Scope CurrentUser\n\n# Install from PowerShell Gallery\nInstall-Module dbatools -Scope CurrentUser -Force\n\n# Import and verify\nImport-Module dbatools\nGet-Command -Module dbatools | Measure-Object\n\n# Should show 500+ commands\nWrite-Host \"`nDBATools installed successfully!\" -ForegroundColor Green\nWrite-Host \"Commands available: $((Get-Command -Module dbatools).Count)\""},
            {"name": "First Connection", "code": "# Your first connection to SQL Server\n$sqlInstance = 'localhost'\n\n# Test the connection\n$result = Test-DbaConnection -SqlInstance $sqlInstance\n$result | Format-List *\n\n# List all databases\nGet-DbaDatabase -SqlInstance $sqlInstance |\n    Select-Object Name, Status, RecoveryModel, SizeMB |\n    Format-Table -AutoSize\n\nWrite-Host \"`nLab is ready!\" -ForegroundColor Green"}
          ]
        },
        {
          "id": "m1s3",
          "title": "1.3 PowerShell Basics Refresher",
          "brief": "<h4>PowerShell Basics Refresher</h4><ul><li>Cmdlets follow Verb-Noun naming: Get-Service, Set-Location</li><li>Pipeline passes objects (not text): Get-Process | Where-Object CPU -gt 50</li><li>Variables use $ prefix: $server = 'SQL-PROD-01'</li><li>Everything is a .NET object with properties and methods</li><li>Get-Help and Get-Member are your best friends</li><li>Tab completion and IntelliSense speed up coding</li></ul>",
          "description": "<p><strong>PowerShell is an object-oriented shell and scripting language built on .NET.</strong> Unlike bash or cmd, PowerShell passes rich objects through the pipeline — not plain text. This makes it incredibly powerful for managing structured data like SQL Server metadata.</p><h4>Core Concepts</h4><ul><li><strong>Cmdlets:</strong> Verb-Noun commands (Get-Service, New-Item, Set-Location)</li><li><strong>Pipeline:</strong> Chain commands with | — objects flow between them</li><li><strong>Variables:</strong> $name = value — dynamically typed</li><li><strong>Objects:</strong> Everything has properties (.Name) and methods (.ToString())</li></ul>",
          "syntax": "# Variables\n$server = 'SQL-PROD-01'\n$databases = @('master', 'msdb', 'model')\n\n# Pipeline\nGet-Service | Where-Object Status -eq 'Running'\n\n# Objects\n$date = Get-Date\n$date.DayOfWeek\n$date.AddDays(-7)\n\n# Help\nGet-Help Get-DbaDatabase -Examples",
          "examples": [
            {"name": "Core Concepts", "code": "# PowerShell fundamentals for DBAs\n\n# Variables\n$sqlInstance = 'localhost\\SQL2019'\n$threshold = 85\n$databases = @('Sales', 'Inventory', 'HR')\n\n# String interpolation\nWrite-Host \"Connecting to: $sqlInstance\"\nWrite-Host \"Alert threshold: $threshold%\"\n\n# Pipeline - objects flow through\nGet-Service *SQL* |\n    Where-Object Status -eq 'Running' |\n    Select-Object Name, Status, StartType |\n    Format-Table -AutoSize\n\n# Hashtable (like a dictionary)\n$config = @{\n    Server    = 'SQL-PROD-01'\n    Database  = 'Production'\n    Timeout   = 30\n}\nWrite-Host \"`nConfig:\" $config.Server"},
            {"name": "Pipeline Power", "code": "# The pipeline is PowerShell's superpower\n\n# Get processes using most CPU\nGet-Process | Sort-Object CPU -Descending |\n    Select-Object -First 5 Name, CPU, WorkingSet64 |\n    Format-Table -AutoSize\n\n# Filter, sort, and format in one pipeline\nGet-ChildItem C:\\SQLBackups -Filter *.bak |\n    Where-Object Length -gt 1GB |\n    Sort-Object LastWriteTime -Descending |\n    Select-Object Name, @{N='SizeGB';E={[math]::Round($_.Length/1GB,2)}}, LastWriteTime\n\n# ForEach-Object for actions\n$servers = 'Server1', 'Server2', 'Server3'\n$servers | ForEach-Object {\n    Write-Host \"Processing: $_\" -ForegroundColor Yellow\n}"},
            {"name": "Get-Help & Get-Member", "code": "# Discovery commands every DBA needs\n\n# Get help on any command\nGet-Help Get-DbaDatabase -Examples\n\n# See what properties an object has\nGet-Service MSSQLSERVER | Get-Member -MemberType Property\n\n# Find commands by verb or noun\nGet-Command -Module dbatools -Verb Get | Measure-Object\nGet-Command -Module dbatools -Noun *Backup*\n\n# Explore object properties\n$db = Get-DbaDatabase -SqlInstance localhost -Database master\n$db | Get-Member -MemberType Property |\n    Select-Object Name, Definition | Format-Table -AutoSize"}
          ]
        }
      ]
    },
    {
      "id": "module2",
      "title": "Module 2: DBATools Module",
      "sections": [
        {
          "id": "m2s1",
          "title": "2.1 Installing DBATools",
          "brief": "<h4>Installing DBATools</h4><ul><li>DBATools is the #1 PowerShell module for SQL Server DBAs</li><li>500+ commands covering every DBA task</li><li>Install: <code>Install-Module dbatools -Scope CurrentUser</code></li><li>Update: <code>Update-Module dbatools</code></li><li>Free, open-source, community-driven</li><li>Works with SQL Server 2005 through 2022</li></ul>",
          "description": "<p><strong>DBATools is a free, open-source PowerShell module containing 500+ commands specifically designed for SQL Server database administration.</strong> It is the most popular SQL Server PowerShell module and is maintained by a large community of DBAs and developers.</p><h4>Why DBATools</h4><ul><li>Covers every common DBA task: backups, restores, migrations, security, monitoring</li><li>Consistent parameter naming across all commands</li><li>Built-in safety features (WhatIf, Confirm)</li><li>Works across SQL Server versions 2005-2022</li></ul>",
          "syntax": "# Install DBATools\nInstall-Module dbatools -Scope CurrentUser\n\n# Update to latest version\nUpdate-Module dbatools\n\n# Import into session\nImport-Module dbatools\n\n# Check version\nGet-Module dbatools | Select-Object Version\n\n# Count available commands\n(Get-Command -Module dbatools).Count",
          "examples": [
            {"name": "Install & Verify", "code": "# Install DBATools from PowerShell Gallery\nInstall-Module dbatools -Scope CurrentUser -Force\n\n# Import the module\nImport-Module dbatools\n\n# Check version\n$module = Get-Module dbatools\nWrite-Host \"DBATools Version: $($module.Version)\"\nWrite-Host \"Commands Available: $((Get-Command -Module dbatools).Count)\"\n\n# List command categories\nGet-Command -Module dbatools |\n    Group-Object Verb |\n    Sort-Object Count -Descending |\n    Select-Object -First 10 Name, Count |\n    Format-Table -AutoSize"},
            {"name": "Explore Commands", "code": "# Discover DBATools commands by category\n\n# Backup-related commands\nGet-Command -Module dbatools -Noun *Backup* |\n    Select-Object Name | Format-Table\n\n# Database management commands\nGet-Command -Module dbatools -Noun *Database* |\n    Select-Object Name | Format-Table\n\n# Security commands\nGet-Command -Module dbatools -Noun *Login* |\n    Select-Object Name | Format-Table\n\n# Find a command for any task\nFind-DbaCommand -Tag Backup\nFind-DbaCommand -Pattern 'disk space'"},
            {"name": "First DBATools Commands", "code": "# Your first DBATools commands\n$sql = 'localhost'\n\n# Test connection\nTest-DbaConnection -SqlInstance $sql\n\n# Get instance info\nGet-DbaInstanceProperty -SqlInstance $sql |\n    Where-Object Name -in 'ProductVersion','Edition','Collation' |\n    Select-Object Name, Value\n\n# List databases with details\nGet-DbaDatabase -SqlInstance $sql |\n    Select-Object Name, Status, RecoveryModel,\n        @{N='SizeMB';E={[math]::Round($_.SizeMB,2)}} |\n    Format-Table -AutoSize"}
          ]
        },
        {
          "id": "m2s2",
          "title": "2.2 PowerShell Gallery & Modules",
          "brief": "<h4>PowerShell Gallery & Modules</h4><ul><li>PowerShell Gallery (powershellgallery.com) — central module repository</li><li>Find-Module to search for modules</li><li>Install-Module to download and install</li><li>Update-Module to get latest versions</li><li>Key DBA modules: dbatools, SqlServer, PSWindowsUpdate, Pester</li><li>-Scope CurrentUser avoids needing admin rights</li></ul>",
          "description": "<p><strong>The PowerShell Gallery is the central repository for PowerShell modules, scripts, and DSC resources.</strong> It works like NuGet for .NET or npm for Node.js — a trusted source for community and Microsoft-published packages.</p><h4>Key Modules for DBAs</h4><ul><li><strong>dbatools:</strong> 500+ SQL Server administration commands</li><li><strong>SqlServer:</strong> Microsoft's official SQL Server module (SMO-based)</li><li><strong>PSWindowsUpdate:</strong> Manage Windows Updates remotely</li><li><strong>Pester:</strong> Testing framework for PowerShell scripts</li></ul>",
          "syntax": "# Search the gallery\nFind-Module -Name *sql* -Repository PSGallery\n\n# Install a module\nInstall-Module SqlServer -Scope CurrentUser\n\n# List installed modules\nGet-InstalledModule | Format-Table Name, Version\n\n# Update all modules\nGet-InstalledModule | Update-Module\n\n# Remove a module\nUninstall-Module -Name OldModule",
          "examples": [
            {"name": "Gallery Basics", "code": "# Working with PowerShell Gallery\n\n# Search for SQL-related modules\nFind-Module -Name *sql* -Repository PSGallery |\n    Select-Object -First 10 Name, Version, Description |\n    Format-Table -AutoSize\n\n# Check what's installed\nGet-InstalledModule |\n    Select-Object Name, Version, InstalledDate |\n    Format-Table -AutoSize\n\n# Get module details\nFind-Module dbatools | Select-Object *"},
            {"name": "Module Management", "code": "# Managing PowerShell modules\n\n# Install key DBA modules\n$modules = @('dbatools', 'SqlServer', 'PSWindowsUpdate')\n\nforeach ($mod in $modules) {\n    if (-not (Get-Module -ListAvailable -Name $mod)) {\n        Write-Host \"Installing $mod...\" -ForegroundColor Yellow\n        Install-Module $mod -Scope CurrentUser -Force\n    } else {\n        Write-Host \"$mod already installed\" -ForegroundColor Green\n    }\n}\n\n# Check versions\nGet-InstalledModule -Name $modules |\n    Select-Object Name, Version |\n    Format-Table -AutoSize"},
            {"name": "Module Versions", "code": "# Managing module versions\n\n# Check for updates\nGet-InstalledModule | ForEach-Object {\n    $installed = $_.Version\n    $online = (Find-Module $_.Name -ErrorAction SilentlyContinue).Version\n    [PSCustomObject]@{\n        Module    = $_.Name\n        Installed = $installed\n        Available = $online\n        Update    = if ($online -gt $installed) {'Yes'} else {'Current'}\n    }\n} | Format-Table -AutoSize\n\n# Update a specific module\nUpdate-Module dbatools -Force"}
          ]
        },
        {
          "id": "m2s3",
          "title": "2.3 Functions in PowerShell",
          "brief": "<h4>Functions in PowerShell</h4><ul><li>Functions encapsulate reusable logic: <code>function Verb-Noun { }</code></li><li>Use param() block for parameters with types and validation</li><li>Advanced functions add [CmdletBinding()] for -Verbose, -WhatIf support</li><li>Follow Verb-Noun naming convention (Get-, Set-, New-, Remove-)</li><li>Pipeline input with ValueFromPipeline</li><li>Return objects, not formatted text</li></ul>",
          "description": "<p><strong>Functions are reusable blocks of code that accept parameters and return results.</strong> In PowerShell, well-written functions follow the Verb-Noun naming convention and support pipeline input, making them behave like native cmdlets.</p><h4>Function Types</h4><ul><li><strong>Basic functions:</strong> Simple param() block with logic</li><li><strong>Advanced functions:</strong> [CmdletBinding()] adds -Verbose, -WhatIf, -Confirm</li><li><strong>Pipeline functions:</strong> Process input from the pipeline with Begin/Process/End</li></ul>",
          "syntax": "# Basic function\nfunction Get-SqlHealth {\n    param(\n        [string]$SqlInstance = 'localhost'\n    )\n    Test-DbaConnection -SqlInstance $SqlInstance\n}\n\n# Advanced function\nfunction Backup-AllDatabases {\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory)]\n        [string]$SqlInstance,\n        [string]$Path = 'C:\\Backups'\n    )\n    # Logic here\n}",
          "examples": [
            {"name": "Basic Function", "code": "# Creating a basic DBA function\nfunction Get-SqlHealth {\n    param(\n        [string[]]$SqlInstance = 'localhost'\n    )\n\n    foreach ($server in $SqlInstance) {\n        $connection = Test-DbaConnection -SqlInstance $server\n        $databases = Get-DbaDatabase -SqlInstance $server\n\n        [PSCustomObject]@{\n            Server     = $server\n            Connected  = $connection.ConnectSuccess\n            Databases  = $databases.Count\n            Online     = ($databases | Where-Object Status -eq 'Normal').Count\n        }\n    }\n}\n\n# Use the function\nGet-SqlHealth -SqlInstance 'localhost' | Format-Table"},
            {"name": "Advanced Function", "code": "# Advanced function with full cmdlet features\nfunction Backup-UserDatabases {\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)]\n        [string[]]$SqlInstance,\n\n        [ValidateSet('Full','Differential','Log')]\n        [string]$Type = 'Full',\n\n        [string]$BackupPath = '\\\\backup-share\\sql'\n    )\n\n    process {\n        foreach ($server in $SqlInstance) {\n            $dbs = Get-DbaDatabase -SqlInstance $server -ExcludeSystem\n            foreach ($db in $dbs) {\n                if ($PSCmdlet.ShouldProcess($db.Name, \"$Type Backup\")) {\n                    Backup-DbaDatabase -SqlInstance $server `\n                        -Database $db.Name -Type $Type -Path $BackupPath\n                }\n            }\n        }\n    }\n}\n\n# Usage with -WhatIf\nBackup-UserDatabases -SqlInstance 'localhost' -WhatIf"},
            {"name": "Pipeline Function", "code": "# Function that accepts pipeline input\nfunction Test-DatabaseBackupAge {\n    [CmdletBinding()]\n    param(\n        [Parameter(Mandatory)]\n        [string]$SqlInstance,\n\n        [int]$MaxAgeDays = 1\n    )\n\n    $cutoff = (Get-Date).AddDays(-$MaxAgeDays)\n\n    Get-DbaLastBackup -SqlInstance $SqlInstance |\n        Where-Object { $_.LastFullBackup -lt $cutoff } |\n        Select-Object SqlInstance, Database, LastFullBackup,\n            @{N='DaysOld';E={\n                [math]::Round(((Get-Date) - $_.LastFullBackup).TotalDays, 1)\n            }}\n}\n\n# Call it\nTest-DatabaseBackupAge -SqlInstance 'localhost' -MaxAgeDays 1 |\n    Format-Table -AutoSize"}
          ]
        }
      ]
    },
    {
      "id": "module3",
      "title": "Module 3: SQL Server Installation",
      "sections": [
        {
          "id": "m3s1",
          "title": "3.1 PowerShell DSC Overview",
          "brief": "<h4>PowerShell DSC Overview</h4><ul><li>DSC = Desired State Configuration — declarative infrastructure</li><li>Define WHAT you want, not HOW to get there</li><li>Idempotent — safe to run multiple times</li><li>Configuration compiles to MOF files</li><li>Push mode (on-demand) or Pull mode (scheduled)</li><li>SqlServerDsc module provides SQL Server resources</li></ul>",
          "description": "<p><strong>PowerShell Desired State Configuration (DSC) is a declarative platform for defining and enforcing system configurations.</strong> Instead of writing step-by-step scripts, you declare the desired end state and DSC ensures the system matches it.</p><h4>Key Concepts</h4><ul><li><strong>Configuration:</strong> A PowerShell script block that defines desired state</li><li><strong>Resources:</strong> Building blocks (WindowsFeature, File, Service, SqlSetup)</li><li><strong>MOF:</strong> Compiled configuration file that LCM reads</li><li><strong>LCM:</strong> Local Configuration Manager — the engine that applies configs</li></ul>",
          "syntax": "# DSC Configuration structure\nConfiguration InstallSQL {\n    Import-DscResource -ModuleName SqlServerDsc\n\n    Node 'localhost' {\n        SqlSetup 'InstallDefaultInstance' {\n            InstanceName = 'MSSQLSERVER'\n            Features     = 'SQLENGINE'\n            SourcePath   = 'D:\\SQLMedia'\n        }\n    }\n}\n\n# Compile and apply\nInstallSQL\nStart-DscConfiguration -Path .\\InstallSQL -Wait -Verbose",
          "examples": [
            {"name": "DSC Basics", "code": "# Understanding DSC structure\n\n# Install the SqlServerDsc module\nInstall-Module SqlServerDsc -Scope CurrentUser -Force\n\n# Check available DSC resources\nGet-DscResource -Module SqlServerDsc |\n    Select-Object Name, ModuleName |\n    Format-Table -AutoSize\n\n# Check current LCM settings\nGet-DscLocalConfigurationManager |\n    Select-Object RefreshMode, ConfigurationMode, RebootNodeIfNeeded"},
            {"name": "Simple DSC Config", "code": "# A simple DSC configuration example\nConfiguration EnsureSQLRunning {\n    Import-DscResource -ModuleName PSDesiredStateConfiguration\n\n    Node 'localhost' {\n        Service SQLServerService {\n            Name        = 'MSSQLSERVER'\n            State       = 'Running'\n            StartupType = 'Automatic'\n        }\n\n        Service SQLAgentService {\n            Name        = 'SQLSERVERAGENT'\n            State       = 'Running'\n            StartupType = 'Automatic'\n            DependsOn   = '[Service]SQLServerService'\n        }\n    }\n}\n\n# Compile the configuration\nEnsureSQLRunning\n\n# Apply it\nStart-DscConfiguration -Path .\\EnsureSQLRunning -Wait -Verbose -Force"},
            {"name": "Test DSC State", "code": "# Test if current state matches desired state\n\n# Test the configuration\n$result = Test-DscConfiguration -Detailed\n\nWrite-Host \"In Desired State: $($result.InDesiredState)\" -ForegroundColor $(if($result.InDesiredState){'Green'}else{'Red'})\n\n# Show resources not in desired state\nif (-not $result.InDesiredState) {\n    $result.ResourcesNotInDesiredState |\n        Select-Object ResourceId, InDesiredState |\n        Format-Table -AutoSize\n}\n\n# Get current configuration status\nGet-DscConfigurationStatus | Select-Object Status, StartDate, Type"}
          ]
        },
        {
          "id": "m3s2",
          "title": "3.2 SQL Server Installation with DSC",
          "brief": "<h4>SQL Server Installation with DSC</h4><ul><li>SqlSetup resource handles full SQL Server installation</li><li>Define instance name, features, service accounts, paths</li><li>Unattended, repeatable installation across servers</li><li>Supports SQLENGINE, AS, IS, RS features</li><li>Configure collation, authentication mode, admin accounts</li><li>Compile MOF then push with Start-DscConfiguration</li></ul>",
          "description": "<p><strong>The SqlSetup DSC resource automates SQL Server installation in a declarative, repeatable way.</strong> Define your desired SQL Server configuration once, then deploy it identically across development, staging, and production environments.</p>",
          "syntax": "Configuration InstallSQLServer {\n    Import-DscResource -ModuleName SqlServerDsc\n\n    Node 'localhost' {\n        SqlSetup 'InstallSQL' {\n            InstanceName        = 'MSSQLSERVER'\n            Features            = 'SQLENGINE,CONN,SSMS'\n            SourcePath          = 'D:\\SQL2019Media'\n            SQLSysAdminAccounts = @('DOMAIN\\DBATeam')\n            SecurityMode        = 'SQL'\n            SAPwd               = $saCredential\n            InstallSharedDir    = 'C:\\Program Files\\Microsoft SQL Server'\n        }\n    }\n}",
          "examples": [
            {"name": "Full SQL Install DSC", "code": "# Complete SQL Server installation via DSC\nConfiguration InstallSQLServer {\n    param(\n        [Parameter(Mandatory)]\n        [PSCredential]$SaCredential\n    )\n\n    Import-DscResource -ModuleName SqlServerDsc\n\n    Node 'localhost' {\n        SqlSetup 'InstallDefaultInstance' {\n            InstanceName          = 'MSSQLSERVER'\n            Features              = 'SQLENGINE'\n            SourcePath            = 'D:\\SQL2019'\n            SQLSysAdminAccounts   = @('BUILTIN\\Administrators')\n            SecurityMode          = 'SQL'\n            SAPwd                 = $SaCredential\n            SQLCollation          = 'SQL_Latin1_General_CP1_CI_AS'\n            InstallSQLDataDir     = 'D:\\SQLData'\n            SQLUserDBDir          = 'D:\\SQLData'\n            SQLUserDBLogDir       = 'E:\\SQLLogs'\n            SQLTempDBDir          = 'F:\\TempDB'\n            SQLBackupDir          = 'G:\\SQLBackups'\n            UpdateEnabled         = 'True'\n            UpdateSource          = 'D:\\Updates'\n        }\n    }\n}\n\n# Compile and apply\n$cred = Get-Credential -Message 'Enter SA password'\nInstallSQLServer -SaCredential $cred\nStart-DscConfiguration -Path .\\InstallSQLServer -Wait -Verbose"},
            {"name": "Named Instance", "code": "# Install a named instance\nConfiguration InstallNamedInstance {\n    Import-DscResource -ModuleName SqlServerDsc\n\n    Node 'localhost' {\n        SqlSetup 'InstallDevInstance' {\n            InstanceName        = 'DEV2019'\n            Features            = 'SQLENGINE'\n            SourcePath          = 'D:\\SQL2019'\n            SQLSysAdminAccounts = @('BUILTIN\\Administrators')\n            SQLCollation        = 'SQL_Latin1_General_CP1_CI_AS'\n            TCPEnabled          = $true\n            NpEnabled           = $false\n        }\n    }\n}\n\n# After installation, verify\nGet-DbaInstanceProperty -SqlInstance 'localhost\\DEV2019' |\n    Where-Object Name -in 'InstanceName','ProductVersion','Edition' |\n    Format-Table Name, Value"},
            {"name": "Post-Install Config", "code": "# Post-installation configuration with DBATools\n$sqlInstance = 'localhost'\n\n# Set max memory (leave 4GB for OS)\n$totalMemGB = (Get-CimInstance Win32_PhysicalMemory | Measure-Object Capacity -Sum).Sum / 1GB\n$sqlMemMB = ($totalMemGB - 4) * 1024\nSet-DbaMaxMemory -SqlInstance $sqlInstance -Max $sqlMemMB\n\n# Configure TempDB (1 file per core, max 8)\n$cores = (Get-CimInstance Win32_Processor).NumberOfCores\n$tempFiles = [math]::Min($cores, 8)\nSet-DbaTempDbConfig -SqlInstance $sqlInstance -DataFileCount $tempFiles -DataFileSize 1024\n\n# Enable common best practices\nSet-DbaSpConfigure -SqlInstance $sqlInstance -Name 'OptimizeAdhocWorkloads' -Value 1\nSet-DbaSpConfigure -SqlInstance $sqlInstance -Name 'CostThresholdForParallelism' -Value 50\n\nWrite-Host 'Post-install configuration complete!' -ForegroundColor Green"}
          ]
        },
        {
          "id": "m3s3",
          "title": "3.3 Installing SSMS & Applying Patches",
          "brief": "<h4>Installing SSMS & Applying Patches</h4><ul><li>Download SSMS installer silently with Invoke-WebRequest</li><li>Silent install: Start-Process with /Quiet /NoRestart switches</li><li>Apply SQL Server Cumulative Updates with Update-DbaInstance</li><li>Check current patch level: Get-DbaBuild</li><li>Automate patching across multiple servers</li><li>Schedule maintenance windows for patching</li></ul>",
          "description": "<p><strong>Automating SSMS installation and SQL Server patching ensures consistent environments and reduces manual effort.</strong> DBATools provides Update-DbaInstance to apply cumulative updates and service packs across multiple servers.</p>",
          "syntax": "# Download and install SSMS silently\n$url = 'https://aka.ms/ssmsfullsetup'\n$installer = 'C:\\Temp\\SSMS-Setup.exe'\nInvoke-WebRequest -Uri $url -OutFile $installer\nStart-Process $installer -ArgumentList '/Quiet /NoRestart' -Wait\n\n# Check SQL Server build/patch level\nGet-DbaBuild -SqlInstance server1\n\n# Apply cumulative update\nUpdate-DbaInstance -ComputerName server1 -Path C:\\Patches -Restart",
          "examples": [
            {"name": "Install SSMS", "code": "# Automated SSMS installation\n$ssmsUrl = 'https://aka.ms/ssmsfullsetup'\n$downloadPath = 'C:\\Temp\\SSMS-Setup-ENU.exe'\n\n# Download\nWrite-Host 'Downloading SSMS...' -ForegroundColor Yellow\nInvoke-WebRequest -Uri $ssmsUrl -OutFile $downloadPath\n\n# Silent install\nWrite-Host 'Installing SSMS (silent)...' -ForegroundColor Yellow\n$process = Start-Process -FilePath $downloadPath `\n    -ArgumentList '/install /quiet /norestart' `\n    -Wait -PassThru\n\nif ($process.ExitCode -eq 0) {\n    Write-Host 'SSMS installed successfully!' -ForegroundColor Green\n} else {\n    Write-Host \"Install failed. Exit code: $($process.ExitCode)\" -ForegroundColor Red\n}\n\n# Cleanup\nRemove-Item $downloadPath -Force"},
            {"name": "Check Patch Level", "code": "# Check SQL Server patch levels across servers\n$servers = 'SQL-PROD-01', 'SQL-PROD-02', 'SQL-DR-01'\n\n# Get current build info\nGet-DbaBuild -SqlInstance $servers |\n    Select-Object SqlInstance, Build, NameLevel, SPLevel, CULevel, KBLevel |\n    Format-Table -AutoSize\n\n# Check if servers are up to date\nTest-DbaBuild -SqlInstance $servers -MaxBehind '1CU' |\n    Select-Object SqlInstance, BuildLevel, Compliant |\n    Format-Table -AutoSize"},
            {"name": "Apply Patches", "code": "# Apply SQL Server cumulative updates\n$servers = 'SQL-DEV-01', 'SQL-TEST-01'\n$patchPath = '\\\\fileserver\\patches\\SQL2019\\CU25'\n\n# Check current state\nWrite-Host '=== Before Patching ===' -ForegroundColor Cyan\nGet-DbaBuild -SqlInstance $servers | Format-Table SqlInstance, Build, CULevel\n\n# Apply update (with restart)\nUpdate-DbaInstance -ComputerName $servers `\n    -Path $patchPath `\n    -Restart `\n    -Confirm:$false\n\n# Verify after patching\nWrite-Host '`n=== After Patching ===' -ForegroundColor Cyan\nGet-DbaBuild -SqlInstance $servers | Format-Table SqlInstance, Build, CULevel"}
          ]
        }
      ]
    },
    {
      "id": "module4",
      "title": "Module 4: Windows Update with PowerShell",
      "sections": [
        {
          "id": "m4s1",
          "title": "4.1 PSWindowsUpdate Module",
          "brief": "<h4>PSWindowsUpdate Module</h4><ul><li>PSWindowsUpdate — manage Windows Updates via PowerShell</li><li>Install: <code>Install-Module PSWindowsUpdate</code></li><li>Get-WindowsUpdate — scan for available updates</li><li>Install-WindowsUpdate — download and install</li><li>Works remotely with -ComputerName parameter</li><li>Filter by KB number, category, or severity</li></ul>",
          "description": "<p><strong>The PSWindowsUpdate module provides PowerShell cmdlets to manage Windows Updates on local and remote computers.</strong> This is essential for DBAs who need to patch SQL Server hosts during maintenance windows without using WSUS or SCCM.</p>",
          "syntax": "# Install the module\nInstall-Module PSWindowsUpdate -Scope CurrentUser -Force\n\n# Scan for updates\nGet-WindowsUpdate\n\n# Install all updates\nInstall-WindowsUpdate -AcceptAll -AutoReboot\n\n# Install specific KB\nGet-WindowsUpdate -KBArticleID KB5001234 -Install\n\n# Remote computer\nGet-WindowsUpdate -ComputerName Server01",
          "examples": [
            {"name": "Scan Updates", "code": "# Scan for available Windows Updates\nImport-Module PSWindowsUpdate\n\n# Get all available updates\n$updates = Get-WindowsUpdate\n\nWrite-Host \"Available Updates: $($updates.Count)\" -ForegroundColor Cyan\n$updates | Select-Object KB, Size, Title | Format-Table -AutoSize\n\n# Filter by category\nGet-WindowsUpdate -Category 'Security Updates' |\n    Select-Object KB, Title, Size |\n    Format-Table -AutoSize\n\n# Check update history\nGet-WUHistory -Last 10 |\n    Select-Object Date, Title, Result |\n    Format-Table -AutoSize"},
            {"name": "Install Updates", "code": "# Install Windows Updates with control\n\n# Install all updates (no reboot)\nInstall-WindowsUpdate -AcceptAll -IgnoreReboot\n\n# Install only critical/security updates\nInstall-WindowsUpdate -Category 'Security Updates' -AcceptAll\n\n# Exclude specific KBs (known problematic)\n$excludeKBs = @('KB5001234', 'KB5005678')\nInstall-WindowsUpdate -NotKBArticleID $excludeKBs -AcceptAll\n\n# Install with scheduled reboot\nInstall-WindowsUpdate -AcceptAll -ScheduleReboot (Get-Date '02:00 AM')"},
            {"name": "Remote Updates", "code": "# Manage updates on remote SQL Server hosts\n$sqlHosts = 'SQL-HOST-01', 'SQL-HOST-02', 'SQL-HOST-03'\n\n# Scan all hosts for pending updates\nforeach ($host in $sqlHosts) {\n    Write-Host \"`n=== $host ===\" -ForegroundColor Cyan\n    $updates = Get-WindowsUpdate -ComputerName $host\n    Write-Host \"Pending updates: $($updates.Count)\"\n    $updates | Select-Object KB, Title | Format-Table -AutoSize\n}\n\n# Install on remote hosts during maintenance window\nInvoke-Command -ComputerName $sqlHosts -ScriptBlock {\n    Import-Module PSWindowsUpdate\n    Install-WindowsUpdate -AcceptAll -IgnoreReboot\n}"}
          ]
        },
        {
          "id": "m4s2",
          "title": "4.2 Automating Windows Updates",
          "brief": "<h4>Automating Windows Updates</h4><ul><li>Schedule patching during maintenance windows</li><li>Stop SQL services before OS patching</li><li>Patch multiple servers in sequence (rolling updates)</li><li>Generate compliance reports after patching</li><li>Integrate with Task Scheduler for unattended execution</li><li>Handle reboots gracefully with service verification</li></ul>",
          "description": "<p><strong>Automating Windows Updates for SQL Server hosts requires careful orchestration.</strong> You need to stop SQL services gracefully, apply patches, reboot if needed, verify services restart, and confirm SQL Server is healthy — all without manual intervention.</p>",
          "syntax": "# Automated patching workflow\n# 1. Stop SQL services\nStop-DbaService -ComputerName server1 -Type Engine\n\n# 2. Apply updates\nInstall-WindowsUpdate -ComputerName server1 -AcceptAll -AutoReboot\n\n# 3. Wait for reboot\nWait-DbaInstance -SqlInstance server1 -Timeout 600\n\n# 4. Verify health\nTest-DbaConnection -SqlInstance server1",
          "examples": [
            {"name": "Maintenance Window", "code": "# Complete maintenance window automation\nfunction Invoke-PatchingWindow {\n    param(\n        [string[]]$Servers,\n        [string]$MaintenanceStart = '02:00',\n        [string]$MaintenanceEnd = '06:00'\n    )\n\n    foreach ($server in $Servers) {\n        Write-Host \"`n=== Patching $server ===\" -ForegroundColor Cyan\n\n        # Step 1: Stop SQL Agent\n        Write-Host '  Stopping SQL Agent...' -ForegroundColor Yellow\n        Stop-DbaService -ComputerName $server -Type Agent -Force\n\n        # Step 2: Apply Windows Updates\n        Write-Host '  Installing updates...' -ForegroundColor Yellow\n        Install-WindowsUpdate -ComputerName $server -AcceptAll -AutoReboot\n\n        # Step 3: Wait for server to come back\n        Write-Host '  Waiting for SQL Server...' -ForegroundColor Yellow\n        Start-Sleep -Seconds 120\n        $null = Test-DbaConnection -SqlInstance $server\n\n        # Step 4: Verify\n        Write-Host '  Verifying health...' -ForegroundColor Yellow\n        Test-DbaConnection -SqlInstance $server\n        Write-Host \"  $server patched successfully!\" -ForegroundColor Green\n    }\n}\n\n# Execute\nInvoke-PatchingWindow -Servers 'SQL-DEV-01','SQL-DEV-02'"},
            {"name": "Compliance Report", "code": "# Generate patching compliance report\n$servers = 'SQL-PROD-01', 'SQL-PROD-02', 'SQL-DR-01'\n\n$report = foreach ($server in $servers) {\n    $pending = Get-WindowsUpdate -ComputerName $server\n    $lastInstall = Get-WUHistory -ComputerName $server -Last 1\n\n    [PSCustomObject]@{\n        Server         = $server\n        PendingUpdates = $pending.Count\n        LastPatchDate  = $lastInstall.Date\n        DaysSincePatch = ((Get-Date) - $lastInstall.Date).Days\n        Compliant      = if ($pending.Count -eq 0) {'Yes'} else {'No'}\n    }\n}\n\n$report | Format-Table -AutoSize\n\n# Export to CSV\n$report | Export-Csv -Path 'C:\\Reports\\PatchCompliance.csv' -NoTypeInformation\nWrite-Host \"`nReport saved to C:\\Reports\\PatchCompliance.csv\" -ForegroundColor Green"}
          ]
        }
      ]
    },
    {
      "id": "module5",
      "title": "Module 5: Database Management with DBATools",
      "sections": [
        {
          "id": "m5s1",
          "title": "5.1 Creating Databases",
          "brief": "<h4>Creating Databases</h4><ul><li>New-DbaDatabase creates databases with full control</li><li>Specify data/log file paths, sizes, and growth settings</li><li>Set recovery model, owner, and compatibility level</li><li>Create multiple databases in a loop</li><li>Template-based creation for consistency</li><li>Verify with Get-DbaDatabase after creation</li></ul>",
          "description": "<p><strong>Creating databases with PowerShell gives you repeatable, documented, and consistent database provisioning.</strong> Instead of clicking through SSMS wizards, you define exact specifications in code that can be version-controlled and reused.</p>",
          "syntax": "# Create a database\nNew-DbaDatabase -SqlInstance server1 -Name 'ProductionDB'\n\n# With full options\nNew-DbaDatabase -SqlInstance server1 -Name 'SalesDB' `\n    -RecoveryModel Full `\n    -Owner 'sa' `\n    -DataFilePath 'D:\\Data' `\n    -LogFilePath 'E:\\Logs'\n\n# Set recovery model\nSet-DbaDbRecoveryModel -SqlInstance server1 -Database SalesDB -RecoveryModel Full",
          "examples": [
            {"name": "Create Database", "code": "# Create a new database with DBATools\n$sqlInstance = 'localhost'\n\n# Simple creation\nNew-DbaDatabase -SqlInstance $sqlInstance -Name 'TestDB_Demo'\n\n# With specific settings\nNew-DbaDatabase -SqlInstance $sqlInstance `\n    -Name 'ProductionDB' `\n    -RecoveryModel Full `\n    -Owner 'sa'\n\n# Verify creation\nGet-DbaDatabase -SqlInstance $sqlInstance -Database 'ProductionDB' |\n    Select-Object Name, Status, RecoveryModel, Owner, SizeMB |\n    Format-List"},
            {"name": "Bulk Create", "code": "# Create multiple databases from a template\n$sqlInstance = 'localhost'\n\n$databases = @(\n    @{Name='Sales_2024'; Recovery='Full'},\n    @{Name='Inventory_2024'; Recovery='Full'},\n    @{Name='Reporting_2024'; Recovery='Simple'},\n    @{Name='Archive_2024'; Recovery='Simple'}\n)\n\nforeach ($db in $databases) {\n    Write-Host \"Creating $($db.Name)...\" -ForegroundColor Yellow\n    New-DbaDatabase -SqlInstance $sqlInstance `\n        -Name $db.Name `\n        -RecoveryModel $db.Recovery `\n        -Owner 'sa'\n}\n\n# Verify all created\nGet-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem |\n    Select-Object Name, RecoveryModel, CreateDate |\n    Format-Table -AutoSize"},
            {"name": "Configure After Create", "code": "# Create and configure a database\n$sqlInstance = 'localhost'\n$dbName = 'AppDB_Production'\n\n# Create\nNew-DbaDatabase -SqlInstance $sqlInstance -Name $dbName\n\n# Set recovery model\nSet-DbaDbRecoveryModel -SqlInstance $sqlInstance `\n    -Database $dbName -RecoveryModel Full -Confirm:$false\n\n# Set owner\nSet-DbaDbOwner -SqlInstance $sqlInstance `\n    -Database $dbName -TargetLogin 'sa'\n\n# Set compatibility level\nSet-DbaDbCompatibility -SqlInstance $sqlInstance `\n    -Database $dbName -Compatibility Version150\n\n# Verify final state\nGet-DbaDatabase -SqlInstance $sqlInstance -Database $dbName |\n    Select-Object Name, Owner, RecoveryModel, Compatibility, SizeMB |\n    Format-List"}
          ]
        },
        {
          "id": "m5s2",
          "title": "5.2 Database Properties & Configuration",
          "brief": "<h4>Database Properties & Configuration</h4><ul><li>Get-DbaDatabase returns rich objects with all properties</li><li>Recovery Model: Full, Simple, Bulk-Logged</li><li>Compatibility Level: matches SQL Server version features</li><li>Auto-shrink should always be OFF (performance killer)</li><li>Page Verify should be CHECKSUM (data integrity)</li><li>Query Store settings for performance monitoring</li></ul>",
          "description": "<p><strong>Understanding and managing database properties is a core DBA responsibility.</strong> PowerShell lets you audit and enforce configuration standards across hundreds of databases simultaneously.</p>",
          "syntax": "# Get database properties\nGet-DbaDatabase -SqlInstance server1 |\n    Select-Object Name, RecoveryModel, Compatibility, PageVerify\n\n# Set properties\nSet-DbaDbRecoveryModel -SqlInstance server1 -Database mydb -RecoveryModel Full\nSet-DbaDbCompatibility -SqlInstance server1 -Database mydb -Compatibility Version150\n\n# Check for bad settings\nGet-DbaDatabase -SqlInstance server1 |\n    Where-Object AutoShrink -eq $true",
          "examples": [
            {"name": "Audit Properties", "code": "# Audit database properties across servers\n$sqlInstance = 'localhost'\n\nGet-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem |\n    Select-Object Name,\n        RecoveryModel,\n        @{N='CompatLevel';E={$_.Compatibility}},\n        PageVerify,\n        AutoShrink,\n        AutoClose,\n        @{N='SizeMB';E={[math]::Round($_.SizeMB,2)}} |\n    Format-Table -AutoSize\n\n# Flag databases with bad settings\nWrite-Host \"`n=== Configuration Warnings ===\" -ForegroundColor Yellow\nGet-DbaDatabase -SqlInstance $sqlInstance |\n    Where-Object { $_.AutoShrink -eq $true -or $_.PageVerify -ne 'Checksum' } |\n    Select-Object Name, AutoShrink, PageVerify"},
            {"name": "Fix Settings", "code": "# Fix common misconfigurations\n$sqlInstance = 'localhost'\n\n# Disable auto-shrink on all databases\nGet-DbaDatabase -SqlInstance $sqlInstance |\n    Where-Object AutoShrink -eq $true |\n    ForEach-Object {\n        Write-Host \"Fixing AutoShrink on $($_.Name)\" -ForegroundColor Yellow\n        $_.AutoShrink = $false\n        $_.Alter()\n    }\n\n# Set Page Verify to CHECKSUM\nGet-DbaDatabase -SqlInstance $sqlInstance |\n    Where-Object PageVerify -ne 'Checksum' |\n    ForEach-Object {\n        Write-Host \"Fixing PageVerify on $($_.Name)\" -ForegroundColor Yellow\n        $_.PageVerify = 'Checksum'\n        $_.Alter()\n    }\n\nWrite-Host \"`nAll databases fixed!\" -ForegroundColor Green"},
            {"name": "Space Report", "code": "# Database space usage report\n$sqlInstance = 'localhost'\n\n# Get space info\nGet-DbaDbSpace -SqlInstance $sqlInstance |\n    Select-Object Database, FileName, FileType,\n        @{N='SizeMB';E={[math]::Round($_.Size.Megabyte,2)}},\n        @{N='UsedMB';E={[math]::Round($_.Used.Megabyte,2)}},\n        @{N='FreeMB';E={[math]::Round($_.Available.Megabyte,2)}},\n        PercentUsed |\n    Format-Table -AutoSize\n\n# Find databases with low free space\nGet-DbaDbSpace -SqlInstance $sqlInstance |\n    Where-Object PercentUsed -gt 90 |\n    Select-Object Database, FileName, PercentUsed |\n    Format-Table -AutoSize"}
          ]
        },
        {
          "id": "m5s3",
          "title": "5.3 Copy & Remove Databases",
          "brief": "<h4>Copy & Remove Databases</h4><ul><li>Copy-DbaDatabase — migrate databases between instances</li><li>Methods: BackupRestore (recommended) or DetachAttach</li><li>Copies data, schema, permissions, and objects</li><li>Remove-DbaDatabase — drop databases safely</li><li>Use -WhatIf to preview destructive operations</li><li>SharedPath required for cross-server backup/restore</li></ul>",
          "description": "<p><strong>Copying databases between SQL Server instances is a common DBA task for migrations, refreshing dev/test environments, and disaster recovery setup.</strong> DBATools makes this a single command with Copy-DbaDatabase.</p>",
          "syntax": "# Copy database between servers\nCopy-DbaDatabase -Source server1 -Destination server2 `\n    -Database 'SalesDB' -BackupRestore -SharedPath '\\\\share\\temp'\n\n# Remove a database\nRemove-DbaDatabase -SqlInstance server1 -Database 'OldDB' -Confirm:$false\n\n# Copy with new name\nCopy-DbaDatabase -Source server1 -Destination server1 `\n    -Database 'ProdDB' -DestinationDatabase 'ProdDB_Copy' `\n    -BackupRestore -SharedPath 'C:\\Temp'",
          "examples": [
            {"name": "Copy Database", "code": "# Copy a database to another server\n$source = 'SQL-PROD-01'\n$destination = 'SQL-DEV-01'\n$sharePath = '\\\\fileserver\\dbcopy'\n\n# Copy using backup/restore method\nCopy-DbaDatabase -Source $source `\n    -Destination $destination `\n    -Database 'SalesDB' `\n    -BackupRestore `\n    -SharedPath $sharePath `\n    -Force\n\n# Verify the copy\nGet-DbaDatabase -SqlInstance $destination -Database 'SalesDB' |\n    Select-Object Name, Status, SizeMB, CreateDate"},
            {"name": "Refresh Dev/Test", "code": "# Refresh dev environment from production\n$prod = 'SQL-PROD-01'\n$dev = 'SQL-DEV-01'\n$share = '\\\\fileserver\\refresh'\n$databases = 'Sales', 'Inventory', 'CRM'\n\nforeach ($db in $databases) {\n    Write-Host \"Refreshing $db...\" -ForegroundColor Yellow\n\n    # Remove old dev copy\n    Remove-DbaDatabase -SqlInstance $dev -Database $db -Confirm:$false\n\n    # Copy fresh from prod\n    Copy-DbaDatabase -Source $prod -Destination $dev `\n        -Database $db -BackupRestore -SharedPath $share -Force\n\n    Write-Host \"  $db refreshed!\" -ForegroundColor Green\n}\n\n# Verify all databases\nGet-DbaDatabase -SqlInstance $dev -Database $databases |\n    Select-Object Name, Status, SizeMB | Format-Table"},
            {"name": "Remove Databases", "code": "# Safely remove databases\n$sqlInstance = 'localhost'\n\n# Preview what would be removed (WhatIf)\nRemove-DbaDatabase -SqlInstance $sqlInstance `\n    -Database 'TestDB_Old', 'TempProject_2023' -WhatIf\n\n# Actually remove (with confirmation)\nRemove-DbaDatabase -SqlInstance $sqlInstance `\n    -Database 'TestDB_Old' -Confirm:$false\n\n# Remove all databases matching a pattern\nGet-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem |\n    Where-Object Name -like '*_test_*' |\n    Remove-DbaDatabase -Confirm:$false\n\nWrite-Host 'Cleanup complete!' -ForegroundColor Green"}
          ]
        }
      ]
    },
    {
      "id": "module6",
      "title": "Module 6: Backups & Restore",
      "sections": [
        {
          "id": "m6s1",
          "title": "6.1 Backup Databases with DBATools",
          "brief": "<h4>Backup Databases with DBATools</h4><ul><li>Backup-DbaDatabase — full, differential, and log backups</li><li>-CompressBackup for smaller backup files</li><li>-Verify to validate backup integrity</li><li>-ReplaceInName for timestamped file names</li><li>Backup all user databases in one command</li><li>Network path support for centralized backup storage</li></ul>",
          "description": "<p><strong>Database backups are the most critical responsibility of any DBA.</strong> DBATools simplifies backup operations with Backup-DbaDatabase, supporting all backup types with compression, verification, and flexible naming.</p>",
          "syntax": "# Full backup\nBackup-DbaDatabase -SqlInstance server1 -Database mydb -Type Full\n\n# Compressed backup to network share\nBackup-DbaDatabase -SqlInstance server1 -Database mydb `\n    -Path '\\\\backup\\sql' -CompressBackup -Verify\n\n# All user databases\nBackup-DbaDatabase -SqlInstance server1 -Type Full -CompressBackup\n\n# Transaction log backup\nBackup-DbaDatabase -SqlInstance server1 -Database mydb -Type Log",
          "examples": [
            {"name": "Full Backup", "code": "# Full database backup with best practices\n$sqlInstance = 'localhost'\n$backupPath = 'C:\\SQLBackups'\n\n# Single database backup\nBackup-DbaDatabase -SqlInstance $sqlInstance `\n    -Database 'SalesDB' `\n    -Path $backupPath `\n    -Type Full `\n    -CompressBackup `\n    -Verify `\n    -ReplaceInName\n\n# Backup ALL user databases\nBackup-DbaDatabase -SqlInstance $sqlInstance `\n    -Path $backupPath `\n    -Type Full `\n    -CompressBackup `\n    -Verify\n\n# Check backup results\nGet-DbaLastBackup -SqlInstance $sqlInstance |\n    Select-Object Database, LastFullBackup, LastFullBackupSize |\n    Format-Table -AutoSize"},
            {"name": "Backup Strategy", "code": "# Implement a full backup strategy\n$sqlInstance = 'localhost'\n$basePath = '\\\\backup-server\\SQLBackups'\n\n# Full backup (weekly - Sunday)\nfunction Invoke-WeeklyFull {\n    Backup-DbaDatabase -SqlInstance $sqlInstance `\n        -Path \"$basePath\\Full\" `\n        -Type Full -CompressBackup -Verify `\n        -ReplaceInName -TimeStampFormat 'yyyyMMdd_HHmmss'\n}\n\n# Differential backup (daily)\nfunction Invoke-DailyDiff {\n    Backup-DbaDatabase -SqlInstance $sqlInstance `\n        -Path \"$basePath\\Diff\" `\n        -Type Differential -CompressBackup `\n        -ReplaceInName -TimeStampFormat 'yyyyMMdd_HHmmss'\n}\n\n# Log backup (every 15 minutes)\nfunction Invoke-LogBackup {\n    Get-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem |\n        Where-Object RecoveryModel -ne 'Simple' |\n        Backup-DbaDatabase -Path \"$basePath\\Log\" `\n            -Type Log -CompressBackup `\n            -ReplaceInName -TimeStampFormat 'yyyyMMdd_HHmmss'\n}\n\n# Execute full backup now\nInvoke-WeeklyFull"},
            {"name": "Copy-Only Backup", "code": "# Copy-only backup (doesn't break backup chain)\n$sqlInstance = 'localhost'\n\n# Copy-only for ad-hoc needs\nBackup-DbaDatabase -SqlInstance $sqlInstance `\n    -Database 'ProductionDB' `\n    -Path 'C:\\Temp\\AdHocBackups' `\n    -Type Full `\n    -CopyOnly `\n    -CompressBackup\n\nWrite-Host 'Copy-only backup complete (LSN chain preserved)' -ForegroundColor Green\n\n# Verify the backup file\nGet-ChildItem 'C:\\Temp\\AdHocBackups' -Filter *.bak |\n    Sort-Object LastWriteTime -Descending |\n    Select-Object -First 3 Name, @{N='SizeMB';E={[math]::Round($_.Length/1MB,2)}}, LastWriteTime |\n    Format-Table -AutoSize"}
          ]
        },
        {
          "id": "m6s2",
          "title": "6.2 Restore Databases",
          "brief": "<h4>Restore Databases</h4><ul><li>Restore-DbaDatabase — restore from backup files</li><li>-WithReplace to overwrite existing database</li><li>-NoRecovery for log shipping / standby scenarios</li><li>-DestinationDataDirectory to relocate files</li><li>Restore to a different database name</li><li>Verify backup before restore with Test-DbaLastBackup</li></ul>",
          "description": "<p><strong>Restoring databases is the other half of your backup strategy — and arguably more important.</strong> A backup is worthless if you cannot restore it. DBATools provides Restore-DbaDatabase with extensive options for every restore scenario.</p>",
          "syntax": "# Simple restore\nRestore-DbaDatabase -SqlInstance server1 -Path 'C:\\Backups\\mydb.bak'\n\n# Restore with replace\nRestore-DbaDatabase -SqlInstance server1 -Path 'C:\\Backups\\mydb.bak' -WithReplace\n\n# Restore to different name\nRestore-DbaDatabase -SqlInstance server1 -Path 'C:\\Backups\\prod.bak' `\n    -DatabaseName 'prod_copy' -DestinationDataDirectory 'D:\\Data'\n\n# Test backup restorability\nTest-DbaLastBackup -SqlInstance server1 -Database mydb",
          "examples": [
            {"name": "Basic Restore", "code": "# Restore a database from backup\n$sqlInstance = 'localhost'\n$backupFile = 'C:\\SQLBackups\\SalesDB_Full.bak'\n\n# Restore with replace (overwrite existing)\nRestore-DbaDatabase -SqlInstance $sqlInstance `\n    -Path $backupFile `\n    -WithReplace `\n    -Verbose\n\n# Verify the restore\nGet-DbaDatabase -SqlInstance $sqlInstance -Database 'SalesDB' |\n    Select-Object Name, Status, CreateDate, SizeMB |\n    Format-List\n\n# Check database is accessible\nInvoke-DbaQuery -SqlInstance $sqlInstance `\n    -Database 'SalesDB' `\n    -Query 'SELECT COUNT(*) AS TableCount FROM sys.tables'"},
            {"name": "Restore as Copy", "code": "# Restore production backup as a dev copy\n$sqlInstance = 'localhost'\n$backupFile = '\\\\backup-server\\SQLBackups\\Full\\ProdDB.bak'\n\n# Restore with new name and location\nRestore-DbaDatabase -SqlInstance $sqlInstance `\n    -Path $backupFile `\n    -DatabaseName 'ProdDB_DevCopy' `\n    -DestinationDataDirectory 'D:\\DevData' `\n    -DestinationLogDirectory 'E:\\DevLogs' `\n    -WithReplace\n\n# Set to simple recovery (it's dev)\nSet-DbaDbRecoveryModel -SqlInstance $sqlInstance `\n    -Database 'ProdDB_DevCopy' -RecoveryModel Simple -Confirm:$false\n\nWrite-Host 'Dev copy restored and configured!' -ForegroundColor Green"},
            {"name": "Test Backups", "code": "# Test that backups are actually restorable\n$sqlInstance = 'localhost'\n\n# Test last backup of all databases\n$results = Test-DbaLastBackup -SqlInstance $sqlInstance\n\n$results | Select-Object Database, FileExists, RestoreResult,\n    @{N='BackupAge';E={\n        [math]::Round(((Get-Date) - $_.BackupFinish).TotalHours, 1)\n    }} |\n    Format-Table -AutoSize\n\n# Flag any failures\n$failures = $results | Where-Object RestoreResult -ne 'Success'\nif ($failures) {\n    Write-Host \"`nFAILED RESTORES:\" -ForegroundColor Red\n    $failures | Select-Object Database, RestoreResult | Format-Table\n} else {\n    Write-Host \"`nAll backups verified successfully!\" -ForegroundColor Green\n}"}
          ]
        },
        {
          "id": "m6s3",
          "title": "6.3 Advanced Restore Scenarios",
          "brief": "<h4>Advanced Restore Scenarios</h4><ul><li>Point-in-time recovery with -RestoreTime parameter</li><li>Restore chain: Full + Diff + Log files in sequence</li><li>-NoRecovery for standby/log shipping setup</li><li>-Continue to apply additional log backups</li><li>Page-level restore for minimal downtime</li><li>Restore from Ola Hallengren backup structure</li></ul>",
          "description": "<p><strong>Advanced restore scenarios go beyond simple full restores.</strong> Point-in-time recovery, log chain restores, and standby configurations are essential skills for handling data corruption, accidental deletions, and disaster recovery.</p>",
          "syntax": "# Point-in-time restore\nRestore-DbaDatabase -SqlInstance server1 -Path 'C:\\Backups' `\n    -DatabaseName 'SalesDB' -RestoreTime '2024-01-15 14:30:00'\n\n# Restore with NoRecovery (for applying more logs)\nRestore-DbaDatabase -SqlInstance server1 -Path 'C:\\Backups\\full.bak' `\n    -NoRecovery -WithReplace\n\n# Continue with log restores\nRestore-DbaDatabase -SqlInstance server1 -Path 'C:\\Backups\\log1.trn' `\n    -Continue -NoRecovery",
          "examples": [
            {"name": "Point-in-Time", "code": "# Point-in-time recovery\n$sqlInstance = 'localhost'\n$backupPath = '\\\\backup-server\\SQLBackups\\SalesDB'\n\n# Restore to a specific point in time\n# (just before the accidental DELETE happened)\n$restoreTime = '2024-01-15 14:30:00'\n\nRestore-DbaDatabase -SqlInstance $sqlInstance `\n    -Path $backupPath `\n    -DatabaseName 'SalesDB_Recovered' `\n    -RestoreTime $restoreTime `\n    -DestinationDataDirectory 'D:\\Recovery' `\n    -WithReplace\n\nWrite-Host \"Database restored to: $restoreTime\" -ForegroundColor Green\n\n# Verify data at that point\nInvoke-DbaQuery -SqlInstance $sqlInstance `\n    -Database 'SalesDB_Recovered' `\n    -Query 'SELECT COUNT(*) AS RowCount FROM dbo.Orders'"},
            {"name": "Log Chain Restore", "code": "# Restore full + differential + transaction logs\n$sqlInstance = 'localhost'\n$dbName = 'CriticalDB'\n\n# Step 1: Restore full backup (NoRecovery)\nRestore-DbaDatabase -SqlInstance $sqlInstance `\n    -Path 'C:\\Backups\\CriticalDB_Full.bak' `\n    -DatabaseName $dbName `\n    -NoRecovery -WithReplace\nWrite-Host 'Full backup restored (NORECOVERY)' -ForegroundColor Yellow\n\n# Step 2: Restore differential (NoRecovery)\nRestore-DbaDatabase -SqlInstance $sqlInstance `\n    -Path 'C:\\Backups\\CriticalDB_Diff.bak' `\n    -Continue -NoRecovery\nWrite-Host 'Differential restored (NORECOVERY)' -ForegroundColor Yellow\n\n# Step 3: Restore transaction logs\n$logs = Get-ChildItem 'C:\\Backups\\Logs' -Filter '*.trn' | Sort-Object Name\nforeach ($log in $logs) {\n    Restore-DbaDatabase -SqlInstance $sqlInstance `\n        -Path $log.FullName -Continue -NoRecovery\n    Write-Host \"  Log applied: $($log.Name)\" -ForegroundColor Gray\n}\n\n# Step 4: Bring database online\nInvoke-DbaQuery -SqlInstance $sqlInstance `\n    -Query \"RESTORE DATABASE [$dbName] WITH RECOVERY\"\nWrite-Host \"`n$dbName is now ONLINE!\" -ForegroundColor Green"},
            {"name": "Ola Hallengren Restore", "code": "# Restore from Ola Hallengren backup folder structure\n$sqlInstance = 'localhost'\n$olaPath = '\\\\backup-server\\SQLBackups\\SQL-PROD-01'\n\n# DBATools understands Ola's folder structure automatically\n# Structure: ServerName\\DatabaseName\\FULL|DIFF|LOG\\*.bak\n\n# Restore latest full + all subsequent logs\nRestore-DbaDatabase -SqlInstance $sqlInstance `\n    -Path \"$olaPath\\SalesDB\" `\n    -MaintenanceSolutionBackup `\n    -DestinationDataDirectory 'D:\\Data' `\n    -WithReplace\n\n# Restore all databases from Ola structure\nGet-ChildItem $olaPath -Directory | ForEach-Object {\n    Write-Host \"Restoring $($_.Name)...\" -ForegroundColor Yellow\n    Restore-DbaDatabase -SqlInstance $sqlInstance `\n        -Path $_.FullName `\n        -MaintenanceSolutionBackup `\n        -WithReplace\n}\n\nWrite-Host \"`nAll databases restored!\" -ForegroundColor Green"}
          ]
        }
      ]
    },
    {
      "id": "module7",
      "title": "Module 7: Security Management",
      "sections": [
        {
          "id": "m7s1",
          "title": "7.1 Logins & Users Management",
          "brief": "<h4>Logins & Users Management</h4><ul><li>Logins = server level | Users = database level</li><li>Get-DbaLogin — list all server logins</li><li>New-DbaLogin — create SQL or Windows logins</li><li>New-DbaDbUser — map login to database user</li><li>Windows Authentication preferred over SQL Auth</li><li>Audit logins across multiple instances</li></ul>",
          "description": "<p><strong>Security management is a critical DBA responsibility.</strong> SQL Server uses a two-level security model: Logins at the server level for authentication, and Users at the database level for authorization. PowerShell lets you manage both efficiently across multiple instances.</p>",
          "syntax": "# List logins\nGet-DbaLogin -SqlInstance server1\n\n# Create a SQL login\nNew-DbaLogin -SqlInstance server1 -Login 'AppUser' -SecurePassword $secPwd\n\n# Create a Windows login\nNew-DbaLogin -SqlInstance server1 -Login 'DOMAIN\\ServiceAccount'\n\n# Map login to database user\nNew-DbaDbUser -SqlInstance server1 -Database mydb -Login 'AppUser'",
          "examples": [
            {"name": "Manage Logins", "code": "# Login management with DBATools\n$sqlInstance = 'localhost'\n\n# List all logins\nGet-DbaLogin -SqlInstance $sqlInstance |\n    Select-Object Name, LoginType, CreateDate, IsDisabled |\n    Format-Table -AutoSize\n\n# Create a new SQL login\n$password = ConvertTo-SecureString 'Str0ngP@ss!' -AsPlainText -Force\nNew-DbaLogin -SqlInstance $sqlInstance `\n    -Login 'AppServiceAccount' `\n    -SecurePassword $password\n\n# Create Windows login\nNew-DbaLogin -SqlInstance $sqlInstance `\n    -Login 'DOMAIN\\SQLServiceTeam'\n\n# Disable a login\nSet-DbaLogin -SqlInstance $sqlInstance `\n    -Login 'OldUser' -Disable"},
            {"name": "Database Users", "code": "# Map logins to database users\n$sqlInstance = 'localhost'\n$database = 'SalesDB'\n\n# Create database user from login\nNew-DbaDbUser -SqlInstance $sqlInstance `\n    -Database $database `\n    -Login 'AppServiceAccount' `\n    -Username 'AppServiceAccount'\n\n# List database users\nGet-DbaDbUser -SqlInstance $sqlInstance -Database $database |\n    Where-Object { -not $_.IsSystemObject } |\n    Select-Object Name, Login, LoginType, HasDbAccess |\n    Format-Table -AutoSize\n\n# Add user to a role\nAdd-DbaDbRoleMember -SqlInstance $sqlInstance `\n    -Database $database `\n    -Role 'db_datareader' `\n    -User 'AppServiceAccount'"},
            {"name": "Audit Logins", "code": "# Audit logins across all servers\n$servers = 'SQL-PROD-01', 'SQL-PROD-02', 'SQL-DR-01'\n\n# Find SA-equivalent logins\nGet-DbaLogin -SqlInstance $servers |\n    Where-Object { $_.IsMember('sysadmin') } |\n    Select-Object SqlInstance, Name, LoginType, IsDisabled |\n    Format-Table -AutoSize\n\n# Find disabled logins\nGet-DbaLogin -SqlInstance $servers |\n    Where-Object IsDisabled |\n    Select-Object SqlInstance, Name, LoginType |\n    Format-Table -AutoSize\n\n# Find logins with no corresponding database user\nGet-DbaLogin -SqlInstance $servers |\n    Where-Object { -not $_.EnumDatabaseMappings() } |\n    Select-Object SqlInstance, Name, LoginType"}
          ]
        },
        {
          "id": "m7s2",
          "title": "7.2 Roles & Permissions",
          "brief": "<h4>Roles & Permissions</h4><ul><li>Server roles: sysadmin, securityadmin, dbcreator, etc.</li><li>Database roles: db_owner, db_datareader, db_datawriter</li><li>Custom roles for principle of least privilege</li><li>Get-DbaServerRole / Get-DbaDbRole for auditing</li><li>Add-DbaDbRoleMember to assign users to roles</li><li>Get-DbaUserPermission for detailed permission reports</li></ul>",
          "description": "<p><strong>Roles and permissions control what users can do within SQL Server.</strong> Following the principle of least privilege, you should grant only the minimum permissions needed for each user or application to function.</p>",
          "syntax": "# Server roles\nGet-DbaServerRoleMember -SqlInstance server1\n\n# Database roles\nGet-DbaDbRoleMember -SqlInstance server1 -Database mydb\n\n# Add to role\nAdd-DbaDbRoleMember -SqlInstance server1 -Database mydb `\n    -Role 'db_datareader' -User 'AppUser'\n\n# Get all permissions\nGet-DbaUserPermission -SqlInstance server1 -Database mydb",
          "examples": [
            {"name": "Role Management", "code": "# Manage roles and memberships\n$sqlInstance = 'localhost'\n$database = 'SalesDB'\n\n# List server role members\nGet-DbaServerRoleMember -SqlInstance $sqlInstance |\n    Select-Object Role, Name, Login |\n    Format-Table -AutoSize\n\n# List database role members\nGet-DbaDbRoleMember -SqlInstance $sqlInstance -Database $database |\n    Select-Object Role, UserName, Login |\n    Format-Table -AutoSize\n\n# Add user to multiple roles\n$roles = 'db_datareader', 'db_datawriter'\nforeach ($role in $roles) {\n    Add-DbaDbRoleMember -SqlInstance $sqlInstance `\n        -Database $database -Role $role -User 'AppServiceAccount'\n    Write-Host \"Added to $role\" -ForegroundColor Green\n}"},
            {"name": "Permission Report", "code": "# Comprehensive permission audit\n$sqlInstance = 'localhost'\n\n# Get all user permissions\nGet-DbaUserPermission -SqlInstance $sqlInstance |\n    Select-Object SqlInstance, Object, Type, Member, RoleSecurableClass |\n    Format-Table -AutoSize\n\n# Export to CSV for compliance\nGet-DbaUserPermission -SqlInstance $sqlInstance |\n    Export-Csv 'C:\\Reports\\PermissionAudit.csv' -NoTypeInformation\n\nWrite-Host 'Permission audit exported!' -ForegroundColor Green"},
            {"name": "Least Privilege", "code": "# Implement least privilege access\n$sqlInstance = 'localhost'\n$database = 'ProductionDB'\n\n# Create a custom role with specific permissions\nInvoke-DbaQuery -SqlInstance $sqlInstance -Database $database -Query @\"\nCREATE ROLE [AppReadOnly];\nGRANT SELECT ON SCHEMA::dbo TO [AppReadOnly];\nDENY DELETE, INSERT, UPDATE ON SCHEMA::dbo TO [AppReadOnly];\n\"@\n\n# Create another role for write access to specific tables\nInvoke-DbaQuery -SqlInstance $sqlInstance -Database $database -Query @\"\nCREATE ROLE [AppOrderWriter];\nGRANT SELECT, INSERT, UPDATE ON dbo.Orders TO [AppOrderWriter];\nGRANT SELECT, INSERT ON dbo.OrderItems TO [AppOrderWriter];\n\"@\n\n# Assign users to custom roles\nAdd-DbaDbRoleMember -SqlInstance $sqlInstance `\n    -Database $database -Role 'AppReadOnly' -User 'ReportingUser'\n\nWrite-Host 'Custom roles created and assigned!' -ForegroundColor Green"}
          ]
        },
        {
          "id": "m7s3",
          "title": "7.3 Copy Logins Between Servers",
          "brief": "<h4>Copy Logins Between Servers</h4><ul><li>Copy-DbaLogin — migrate logins preserving SID and password hash</li><li>Prevents orphaned users on destination server</li><li>Copies server role memberships and permissions</li><li>Export-DbaLogin generates T-SQL scripts for logins</li><li>Essential for DR, migrations, and AG setup</li><li>-ExcludeLogin to skip specific accounts</li></ul>",
          "description": "<p><strong>Copying logins between SQL Server instances is essential for migrations, disaster recovery, and Always On Availability Group setup.</strong> Copy-DbaLogin preserves the SID (Security Identifier) and password hash, preventing orphaned users on the destination.</p>",
          "syntax": "# Copy all logins\nCopy-DbaLogin -Source server1 -Destination server2\n\n# Copy specific logins\nCopy-DbaLogin -Source server1 -Destination server2 -Login 'AppUser','SvcAccount'\n\n# Exclude system logins\nCopy-DbaLogin -Source server1 -Destination server2 -ExcludeLogin 'sa','NT*'\n\n# Export logins to T-SQL script\nExport-DbaLogin -SqlInstance server1 -Path 'C:\\Scripts\\Logins.sql'",
          "examples": [
            {"name": "Copy Logins", "code": "# Copy logins from production to DR server\n$source = 'SQL-PROD-01'\n$destination = 'SQL-DR-01'\n\n# Copy all logins (preserves SID and password)\nCopy-DbaLogin -Source $source -Destination $destination `\n    -ExcludeLogin 'sa' `\n    -Force\n\n# Verify logins match\n$sourceLogins = Get-DbaLogin -SqlInstance $source | Where-Object { -not $_.IsSystemObject }\n$destLogins = Get-DbaLogin -SqlInstance $destination | Where-Object { -not $_.IsSystemObject }\n\nWrite-Host \"Source logins: $($sourceLogins.Count)\"\nWrite-Host \"Destination logins: $($destLogins.Count)\"\n\n# Find any missing\n$missing = Compare-Object $sourceLogins.Name $destLogins.Name\nif ($missing) {\n    Write-Host 'Missing logins:' -ForegroundColor Red\n    $missing | Format-Table\n} else {\n    Write-Host 'All logins synced!' -ForegroundColor Green\n}"},
            {"name": "Export Logins", "code": "# Export logins to T-SQL script (for version control)\n$sqlInstance = 'SQL-PROD-01'\n\n# Export all logins to a SQL script\nExport-DbaLogin -SqlInstance $sqlInstance `\n    -Path 'C:\\Scripts\\AllLogins.sql' `\n    -ExcludeLogin 'sa','##*'\n\nWrite-Host 'Logins exported to C:\\Scripts\\AllLogins.sql' -ForegroundColor Green\n\n# Export specific logins\nExport-DbaLogin -SqlInstance $sqlInstance `\n    -Login 'AppUser', 'ReportUser', 'SvcAccount' `\n    -Path 'C:\\Scripts\\AppLogins.sql'\n\n# View the generated script\nGet-Content 'C:\\Scripts\\AppLogins.sql' | Select-Object -First 20"},
            {"name": "Fix Orphaned Users", "code": "# Find and fix orphaned users\n$sqlInstance = 'localhost'\n\n# Find orphaned users across all databases\n$orphans = Get-DbaDbOrphanUser -SqlInstance $sqlInstance\n\nif ($orphans) {\n    Write-Host \"Found $($orphans.Count) orphaned users:\" -ForegroundColor Yellow\n    $orphans | Select-Object SqlInstance, Database, User | Format-Table -AutoSize\n\n    # Repair orphaned users (match to existing logins)\n    Repair-DbaDbOrphanUser -SqlInstance $sqlInstance\n    Write-Host 'Orphaned users repaired!' -ForegroundColor Green\n} else {\n    Write-Host 'No orphaned users found.' -ForegroundColor Green\n}"}
          ]
        }
      ]
    },
    {
      "id": "module8",
      "title": "Module 8: Database Administration",
      "sections": [
        {
          "id": "m8s1",
          "title": "8.1 Instance Management",
          "brief": "<h4>Instance Management</h4><ul><li>Get-DbaSpConfigure — view all instance settings</li><li>Set-DbaMaxMemory — configure SQL memory limits</li><li>Set-DbaTempDbConfig — optimize TempDB files</li><li>Get-DbaService — manage SQL services</li><li>Get-DbaInstanceProperty — instance metadata</li><li>Best practice configuration in one script</li></ul>",
          "description": "<p><strong>Instance-level configuration determines SQL Server performance and stability.</strong> PowerShell lets you audit and enforce best practices across all your instances consistently.</p>",
          "syntax": "# View sp_configure settings\nGet-DbaSpConfigure -SqlInstance server1\n\n# Set max memory\nSet-DbaMaxMemory -SqlInstance server1 -Max 32768\n\n# Configure TempDB\nSet-DbaTempDbConfig -SqlInstance server1 -DataFileCount 8 -DataFileSize 1024\n\n# Manage services\nGet-DbaService -ComputerName server1\nRestart-DbaService -ComputerName server1 -Type Engine",
          "examples": [
            {"name": "Instance Config", "code": "# Audit instance configuration\n$sqlInstance = 'localhost'\n\n# Key sp_configure settings\n$importantSettings = @(\n    'MaxServerMemory','CostThresholdForParallelism',\n    'MaxDegreeOfParallelism','OptimizeAdhocWorkloads',\n    'DefaultBackupCompression'\n)\n\nGet-DbaSpConfigure -SqlInstance $sqlInstance |\n    Where-Object ConfigName -in $importantSettings |\n    Select-Object ConfigName, RunningValue, DefaultValue,\n        @{N='IsDefault';E={$_.RunningValue -eq $_.DefaultValue}} |\n    Format-Table -AutoSize"},
            {"name": "Best Practices", "code": "# Apply best practice configuration\n$sqlInstance = 'localhost'\n\n# Calculate max memory (total - 4GB for OS)\n$totalMem = (Get-CimInstance Win32_PhysicalMemory |\n    Measure-Object Capacity -Sum).Sum / 1MB\n$sqlMem = $totalMem - 4096\nSet-DbaMaxMemory -SqlInstance $sqlInstance -Max $sqlMem\nWrite-Host \"Max Memory set to: $sqlMem MB\" -ForegroundColor Green\n\n# Set MAXDOP based on cores\n$cores = (Get-CimInstance Win32_Processor).NumberOfLogicalProcessors\n$maxdop = [math]::Min($cores, 8)\nSet-DbaSpConfigure -SqlInstance $sqlInstance `\n    -Name 'MaxDegreeOfParallelism' -Value $maxdop\nWrite-Host \"MAXDOP set to: $maxdop\" -ForegroundColor Green\n\n# Cost threshold for parallelism\nSet-DbaSpConfigure -SqlInstance $sqlInstance `\n    -Name 'CostThresholdForParallelism' -Value 50\n\n# Enable backup compression\nSet-DbaSpConfigure -SqlInstance $sqlInstance `\n    -Name 'DefaultBackupCompression' -Value 1\n\n# Optimize for ad hoc workloads\nSet-DbaSpConfigure -SqlInstance $sqlInstance `\n    -Name 'OptimizeAdhocWorkloads' -Value 1\n\nWrite-Host \"`nBest practices applied!\" -ForegroundColor Green"},
            {"name": "Service Management", "code": "# Manage SQL Server services\n$computerName = 'localhost'\n\n# List all SQL services\nGet-DbaService -ComputerName $computerName |\n    Select-Object ServiceName, State, StartMode, ServiceAccount |\n    Format-Table -AutoSize\n\n# Restart SQL Server Agent\nRestart-DbaService -ComputerName $computerName -Type Agent\n\n# Check service accounts\nGet-DbaService -ComputerName $computerName |\n    Select-Object ServiceName, ServiceAccount |\n    Format-Table -AutoSize\n\n# Verify instance is responding after restart\nTest-DbaConnection -SqlInstance $computerName"}
          ]
        },
        {
          "id": "m8s2",
          "title": "8.2 SQL Agent Jobs",
          "brief": "<h4>SQL Agent Jobs</h4><ul><li>Get-DbaAgentJob — list all jobs with status</li><li>New-DbaAgentJob — create jobs programmatically</li><li>Start-DbaAgentJob — run jobs on demand</li><li>Get-DbaAgentJobHistory — check execution history</li><li>Find failed jobs across multiple servers</li><li>Copy-DbaAgentJob — replicate jobs to other servers</li></ul>",
          "description": "<p><strong>SQL Server Agent is the built-in job scheduler for SQL Server.</strong> Managing Agent jobs with PowerShell lets you create, monitor, and troubleshoot jobs across all your instances from a single script.</p>",
          "syntax": "# List all jobs\nGet-DbaAgentJob -SqlInstance server1\n\n# Find failed jobs\nGet-DbaAgentJob -SqlInstance server1 | Where-Object LastRunOutcome -eq 'Failed'\n\n# Start a job\nStart-DbaAgentJob -SqlInstance server1 -Job 'Backup_All_Databases'\n\n# Get job history\nGet-DbaAgentJobHistory -SqlInstance server1 -Job 'MyJob' -Since '2024-01-01'\n\n# Copy jobs to another server\nCopy-DbaAgentJob -Source server1 -Destination server2",
          "examples": [
            {"name": "Monitor Jobs", "code": "# Monitor SQL Agent jobs across servers\n$servers = 'localhost'\n\n# Get all jobs with status\nGet-DbaAgentJob -SqlInstance $servers |\n    Select-Object SqlInstance, Name, IsEnabled, LastRunDate,\n        LastRunOutcome, NextRunDate |\n    Format-Table -AutoSize\n\n# Find failed jobs in last 24 hours\nWrite-Host \"`n=== Failed Jobs (Last 24h) ===\" -ForegroundColor Red\nGet-DbaAgentJob -SqlInstance $servers |\n    Where-Object {\n        $_.LastRunOutcome -eq 'Failed' -and\n        $_.LastRunDate -gt (Get-Date).AddDays(-1)\n    } |\n    Select-Object SqlInstance, Name, LastRunDate |\n    Format-Table -AutoSize"},
            {"name": "Create Job", "code": "# Create a SQL Agent job programmatically\n$sqlInstance = 'localhost'\n\n# Create the job\n$job = New-DbaAgentJob -SqlInstance $sqlInstance `\n    -Job 'DBA_Daily_BackupCheck' `\n    -Description 'Checks backup status and alerts on failures' `\n    -Category 'Database Maintenance' `\n    -OwnerLogin 'sa'\n\n# Add a job step\nNew-DbaAgentJobStep -SqlInstance $sqlInstance `\n    -Job 'DBA_Daily_BackupCheck' `\n    -StepName 'Check Backups' `\n    -Subsystem TransactSql `\n    -Command @\"\nSELECT d.name, MAX(b.backup_finish_date) AS LastBackup\nFROM sys.databases d\nLEFT JOIN msdb.dbo.backupset b ON d.name = b.database_name\nWHERE d.database_id > 4\nGROUP BY d.name\nHAVING MAX(b.backup_finish_date) < DATEADD(DAY, -1, GETDATE())\n\"@\n\n# Add a schedule (daily at 8 AM)\nNew-DbaAgentSchedule -SqlInstance $sqlInstance `\n    -Job 'DBA_Daily_BackupCheck' `\n    -Schedule 'Daily_8AM' `\n    -FrequencyType Daily `\n    -StartTime '080000'\n\nWrite-Host 'Job created with schedule!' -ForegroundColor Green"},
            {"name": "Job History", "code": "# Analyze job execution history\n$sqlInstance = 'localhost'\n\n# Get recent job history\nGet-DbaAgentJobHistory -SqlInstance $sqlInstance `\n    -StartDate (Get-Date).AddDays(-7) |\n    Where-Object StepName -ne '(Job outcome)' |\n    Select-Object Job, StepName, RunDate, RunDuration, Status |\n    Format-Table -AutoSize\n\n# Job duration trends\nGet-DbaAgentJobHistory -SqlInstance $sqlInstance `\n    -Job 'Backup_All_Databases' -StartDate (Get-Date).AddDays(-30) |\n    Where-Object StepName -eq '(Job outcome)' |\n    Select-Object RunDate,\n        @{N='DurationMin';E={$_.RunDuration / 60}},\n        Status |\n    Format-Table -AutoSize"}
          ]
        },
        {
          "id": "m8s3",
          "title": "8.3 Error Logs & Orphaned Users",
          "brief": "<h4>Error Logs & Orphaned Users</h4><ul><li>Get-DbaErrorLog — search SQL Server error logs</li><li>Filter by date, severity, and text pattern</li><li>Get-DbaDbOrphanUser — find orphaned database users</li><li>Repair-DbaDbOrphanUser — auto-fix orphaned users</li><li>Cycle error logs: Invoke-DbaDbLogShipping sp_cycle_errorlog</li><li>Proactive monitoring with scheduled scripts</li></ul>",
          "description": "<p><strong>SQL Server error logs and orphaned users are common troubleshooting targets for DBAs.</strong> Error logs contain critical information about failures, while orphaned users (database users without matching server logins) cause access issues after migrations or restores.</p>",
          "syntax": "# Read error log\nGet-DbaErrorLog -SqlInstance server1 -After (Get-Date).AddHours(-24)\n\n# Filter for errors only\nGet-DbaErrorLog -SqlInstance server1 | Where-Object { $_.Text -match 'error|fail' }\n\n# Find orphaned users\nGet-DbaDbOrphanUser -SqlInstance server1\n\n# Fix orphaned users\nRepair-DbaDbOrphanUser -SqlInstance server1",
          "examples": [
            {"name": "Error Log Analysis", "code": "# Analyze SQL Server error logs\n$sqlInstance = 'localhost'\n\n# Get errors from last 24 hours\n$errors = Get-DbaErrorLog -SqlInstance $sqlInstance `\n    -After (Get-Date).AddHours(-24)\n\nWrite-Host \"Total log entries (24h): $($errors.Count)\" -ForegroundColor Cyan\n\n# Filter for actual errors (not informational)\n$criticalErrors = $errors | Where-Object {\n    $_.Text -match 'error|fail|corrupt|insufficient' -and\n    $_.Text -notmatch 'without errors|errorlog'\n}\n\nif ($criticalErrors) {\n    Write-Host \"`nCritical entries found: $($criticalErrors.Count)\" -ForegroundColor Red\n    $criticalErrors | Select-Object LogDate, Source, Text |\n        Format-Table -AutoSize -Wrap\n} else {\n    Write-Host 'No critical errors found.' -ForegroundColor Green\n}"},
            {"name": "Orphaned Users", "code": "# Find and fix orphaned database users\n$sqlInstance = 'localhost'\n\n# Find orphaned users across all databases\n$orphans = Get-DbaDbOrphanUser -SqlInstance $sqlInstance\n\nif ($orphans) {\n    Write-Host \"Orphaned users found: $($orphans.Count)\" -ForegroundColor Yellow\n    $orphans | Select-Object SqlInstance, Database, User |\n        Format-Table -AutoSize\n\n    # Attempt automatic repair\n    Write-Host \"`nAttempting repair...\" -ForegroundColor Yellow\n    Repair-DbaDbOrphanUser -SqlInstance $sqlInstance\n\n    # Verify fix\n    $remaining = Get-DbaDbOrphanUser -SqlInstance $sqlInstance\n    if ($remaining) {\n        Write-Host \"Still orphaned (manual fix needed):\" -ForegroundColor Red\n        $remaining | Format-Table\n    } else {\n        Write-Host 'All orphaned users repaired!' -ForegroundColor Green\n    }\n} else {\n    Write-Host 'No orphaned users found.' -ForegroundColor Green\n}"},
            {"name": "Log Monitoring Script", "code": "# Proactive error log monitoring script\n$servers = 'localhost'\n$checkInterval = 60  # minutes\n\nfunction Get-SqlAlerts {\n    param([string[]]$SqlInstances)\n\n    $alerts = foreach ($server in $SqlInstances) {\n        Get-DbaErrorLog -SqlInstance $server `\n            -After (Get-Date).AddMinutes(-$checkInterval) |\n            Where-Object {\n                $_.Text -match 'error|fail|corrupt|deadlock|timeout'\n            } | ForEach-Object {\n                [PSCustomObject]@{\n                    Server  = $server\n                    Time    = $_.LogDate\n                    Source  = $_.Source\n                    Message = $_.Text.Substring(0, [math]::Min(100, $_.Text.Length))\n                }\n            }\n    }\n    return $alerts\n}\n\n# Run the check\n$alerts = Get-SqlAlerts -SqlInstances $servers\nif ($alerts) {\n    Write-Host \"ALERTS FOUND: $($alerts.Count)\" -ForegroundColor Red\n    $alerts | Format-Table -AutoSize -Wrap\n} else {\n    Write-Host \"No alerts in last $checkInterval minutes\" -ForegroundColor Green\n}"}
          ]
        }
      ]
    },
    {
      "id": "module9",
      "title": "Module 9: Replication Automation",
      "sections": [
        {
          "id": "m9s1",
          "title": "9.1 Replication Scenarios",
          "brief": "<h4>Replication Scenarios</h4><ul><li>Transactional Replication — real-time data distribution</li><li>Snapshot Replication — periodic full copies</li><li>Merge Replication — bidirectional sync</li><li>Publisher → Distributor → Subscriber architecture</li><li>Use cases: reporting, data distribution, DR</li><li>Monitor with Get-DbaReplPublication</li></ul>",
          "description": "<p><strong>SQL Server Replication distributes data between servers for reporting, load distribution, and high availability.</strong> Understanding replication types and when to use each is essential for enterprise DBA work.</p>",
          "syntax": "# Check replication status\nGet-DbaReplPublication -SqlInstance server1\nGet-DbaReplSubscription -SqlInstance server1\n\n# Monitor replication latency\nMeasure-DbaReplLatency -SqlInstance server1\n\n# Test replication health\nTest-DbaReplLatency -SqlInstance server1 -PublicationName 'MyPub'",
          "examples": [
            {"name": "Replication Status", "code": "# Check replication configuration and status\n$sqlInstance = 'localhost'\n\n# List publications\nGet-DbaReplPublication -SqlInstance $sqlInstance |\n    Select-Object SqlInstance, DatabaseName, Name, Type |\n    Format-Table -AutoSize\n\n# List subscriptions\nGet-DbaReplSubscription -SqlInstance $sqlInstance |\n    Select-Object PublicationName, SubscriberName, SubscriptionDB, Status |\n    Format-Table -AutoSize\n\n# Check distributor\nGet-DbaReplDistributor -SqlInstance $sqlInstance |\n    Select-Object SqlInstance, IsDistributor, IsPublisher |\n    Format-List"},
            {"name": "Monitor Latency", "code": "# Monitor replication latency\n$publisher = 'SQL-PROD-01'\n\n# Measure latency for all publications\n$latency = Measure-DbaReplLatency -SqlInstance $publisher\n\n$latency | Select-Object PublicationName, SubscriberName,\n    @{N='LatencySeconds';E={$_.Latency.TotalSeconds}},\n    Status |\n    Format-Table -AutoSize\n\n# Alert on high latency\n$threshold = 300  # 5 minutes\n$highLatency = $latency | Where-Object { $_.Latency.TotalSeconds -gt $threshold }\n\nif ($highLatency) {\n    Write-Host \"`nHIGH LATENCY ALERT:\" -ForegroundColor Red\n    $highLatency | Format-Table PublicationName, SubscriberName, Latency\n} else {\n    Write-Host 'All replication within threshold.' -ForegroundColor Green\n}"},
            {"name": "Replication Types", "code": "# Understanding replication types\n$scenarios = @(\n    [PSCustomObject]@{\n        Type='Transactional'\n        UseCase='Real-time reporting server'\n        Latency='Seconds'\n        Direction='One-way'\n    },\n    [PSCustomObject]@{\n        Type='Snapshot'\n        UseCase='Periodic data refresh'\n        Latency='Minutes/Hours'\n        Direction='One-way'\n    },\n    [PSCustomObject]@{\n        Type='Merge'\n        UseCase='Mobile/disconnected clients'\n        Latency='Variable'\n        Direction='Bidirectional'\n    },\n    [PSCustomObject]@{\n        Type='Peer-to-Peer'\n        UseCase='Multi-master scale-out'\n        Latency='Seconds'\n        Direction='Multi-directional'\n    }\n)\n\n$scenarios | Format-Table -AutoSize\n\nWrite-Host \"`nMost common for DBAs: Transactional Replication\" -ForegroundColor Cyan"}
          ]
        },
        {
          "id": "m9s2",
          "title": "9.2 Automating Replication",
          "brief": "<h4>Automating Replication</h4><ul><li>Configure publisher, distributor, subscriber via PowerShell</li><li>Use SMO RMO (Replication Management Objects)</li><li>Create publications and add articles programmatically</li><li>Add subscriptions with push or pull model</li><li>Reinitialize subscriptions when needed</li><li>Script entire replication topology for DR</li></ul>",
          "description": "<p><strong>Automating replication setup with PowerShell ensures consistent, repeatable deployments.</strong> Using SMO's Replication Management Objects (RMO), you can script the entire replication topology — essential for disaster recovery and environment provisioning.</p>",
          "syntax": "# Enable distribution\nInvoke-DbaQuery -SqlInstance server1 -Query \"EXEC sp_adddistributor @distributor = 'server1'\"\n\n# Create publication\nNew-DbaReplPublication -SqlInstance server1 -Database 'SalesDB' `\n    -Name 'Sales_Publication' -Type Transactional\n\n# Add article\nAdd-DbaReplArticle -SqlInstance server1 -Database 'SalesDB' `\n    -Publication 'Sales_Publication' -Name 'Orders'\n\n# Add subscription\nNew-DbaReplSubscription -SqlInstance server1 -Database 'SalesDB' `\n    -Publication 'Sales_Publication' -SubscriberSqlInstance server2",
          "examples": [
            {"name": "Setup Replication", "code": "# Setup transactional replication end-to-end\n$publisher = 'SQL-PROD-01'\n$subscriber = 'SQL-REPORT-01'\n$database = 'SalesDB'\n$pubName = 'Sales_TransRepl'\n\n# Step 1: Configure distribution\nInvoke-DbaQuery -SqlInstance $publisher -Query @\"\n    EXEC sp_adddistributor @distributor = '$publisher'\n    EXEC sp_adddistributiondb @database = 'distribution'\n\"@\nWrite-Host 'Distribution configured' -ForegroundColor Green\n\n# Step 2: Enable database for publishing\nInvoke-DbaQuery -SqlInstance $publisher -Query @\"\n    EXEC sp_replicationdboption\n        @dbname = '$database',\n        @optname = 'publish',\n        @value = 'true'\n\"@\nWrite-Host 'Database enabled for publishing' -ForegroundColor Green\n\n# Step 3: Create publication\nNew-DbaReplPublication -SqlInstance $publisher `\n    -Database $database -Name $pubName -Type Transactional\nWrite-Host 'Publication created' -ForegroundColor Green"},
            {"name": "Add Articles", "code": "# Add tables as articles to a publication\n$publisher = 'SQL-PROD-01'\n$database = 'SalesDB'\n$pubName = 'Sales_TransRepl'\n\n# Get all user tables and add as articles\n$tables = Invoke-DbaQuery -SqlInstance $publisher -Database $database -Query @\"\n    SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES\n    WHERE TABLE_TYPE = 'BASE TABLE' AND TABLE_SCHEMA = 'dbo'\n\"@\n\nforeach ($table in $tables.TABLE_NAME) {\n    Add-DbaReplArticle -SqlInstance $publisher `\n        -Database $database `\n        -Publication $pubName `\n        -Name $table `\n        -Filter $null\n    Write-Host \"  Added article: $table\" -ForegroundColor Gray\n}\n\nWrite-Host \"`n$($tables.Count) articles added to publication\" -ForegroundColor Green"},
            {"name": "Manage Subscriptions", "code": "# Manage replication subscriptions\n$publisher = 'SQL-PROD-01'\n$subscriber = 'SQL-REPORT-01'\n$pubName = 'Sales_TransRepl'\n$database = 'SalesDB'\n\n# Add push subscription\nNew-DbaReplSubscription -SqlInstance $publisher `\n    -Database $database `\n    -Publication $pubName `\n    -SubscriberSqlInstance $subscriber `\n    -SubscriptionDatabase 'SalesDB_Replica' `\n    -Type Push\n\nWrite-Host 'Subscription created' -ForegroundColor Green\n\n# Initialize subscription with snapshot\nInvoke-DbaQuery -SqlInstance $publisher -Query @\"\n    EXEC sp_startpublication_snapshot\n        @publication = '$pubName'\n\"@\n\n# Monitor initialization\nStart-Sleep -Seconds 30\nGet-DbaReplSubscription -SqlInstance $publisher |\n    Select-Object PublicationName, SubscriberName, Status |\n    Format-Table -AutoSize"}
          ]
        },
        {
          "id": "m9s3",
          "title": "9.3 Switch & Wildcard Parameters",
          "brief": "<h4>Switch & Wildcard Parameters</h4><ul><li>Switch parameters: -Force, -Confirm, -WhatIf, -Verbose</li><li>Wildcard filtering: -Database *prod* -Exclude *test*</li><li>Switch statement for multi-condition logic</li><li>-Like and -Match operators for pattern matching</li><li>Combine with DBATools for powerful filtering</li><li>Essential for writing flexible DBA scripts</li></ul>",
          "description": "<p><strong>Switch parameters and wildcard patterns make PowerShell scripts flexible and powerful.</strong> Understanding these features lets you write DBA scripts that handle multiple scenarios without hardcoding values.</p>",
          "syntax": "# Switch parameters\nBackup-DbaDatabase -SqlInstance server1 -CompressBackup -Verify -Force\nRemove-DbaDatabase -SqlInstance server1 -Database 'test' -WhatIf\n\n# Wildcard filtering\nGet-DbaDatabase -SqlInstance server1 -Database *prod*\nGet-DbaDatabase -SqlInstance server1 -ExcludeDatabase *test*, *dev*\n\n# Switch statement\nswitch ($recoveryModel) {\n    'Full'   { 'Log backups required' }\n    'Simple' { 'No log backups needed' }\n    default  { 'Unknown model' }\n}",
          "examples": [
            {"name": "Switch Statement", "code": "# Switch statement for DBA decision logic\n$sqlInstance = 'localhost'\n\n# Get databases and categorize by size\nGet-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem | ForEach-Object {\n    $category = switch ([math]::Round($_.SizeMB)) {\n        {$_ -lt 100}    { 'Small' }\n        {$_ -lt 1024}   { 'Medium' }\n        {$_ -lt 10240}  { 'Large' }\n        default         { 'Very Large' }\n    }\n\n    $backupStrategy = switch ($_.RecoveryModel) {\n        'Full'        { 'Full + Log every 15min' }\n        'Simple'      { 'Full daily only' }\n        'BulkLogged'  { 'Full + Log every 30min' }\n    }\n\n    [PSCustomObject]@{\n        Database = $_.Name\n        SizeMB   = [math]::Round($_.SizeMB, 2)\n        Category = $category\n        Recovery = $_.RecoveryModel\n        Strategy = $backupStrategy\n    }\n} | Format-Table -AutoSize"},
            {"name": "Wildcard Patterns", "code": "# Wildcard patterns for flexible filtering\n$sqlInstance = 'localhost'\n\n# Filter databases by pattern\nWrite-Host '=== Production Databases ===' -ForegroundColor Cyan\nGet-DbaDatabase -SqlInstance $sqlInstance -Database *prod*, *production* |\n    Select-Object Name, SizeMB | Format-Table\n\n# Exclude patterns\nWrite-Host '=== Non-System, Non-Test ===' -ForegroundColor Cyan\nGet-DbaDatabase -SqlInstance $sqlInstance `\n    -ExcludeDatabase *test*, *dev*, *temp* -ExcludeSystem |\n    Select-Object Name, SizeMB | Format-Table\n\n# Using -Like operator\nGet-DbaLogin -SqlInstance $sqlInstance |\n    Where-Object Name -like '*svc*' |\n    Select-Object Name, LoginType | Format-Table\n\n# Using -Match (regex)\nGet-DbaAgentJob -SqlInstance $sqlInstance |\n    Where-Object Name -match '^(Backup|Maint)' |\n    Select-Object Name, IsEnabled | Format-Table"},
            {"name": "Flexible Scripts", "code": "# Writing flexible DBA scripts with parameters\nfunction Invoke-DbaAction {\n    [CmdletBinding(SupportsShouldProcess)]\n    param(\n        [Parameter(Mandatory)]\n        [string]$SqlInstance,\n\n        [string]$DatabaseFilter = '*',\n        [string[]]$ExcludeDatabase = @('tempdb'),\n\n        [switch]$IncludeSystem,\n        [switch]$CompressBackup,\n        [switch]$Force\n    )\n\n    $params = @{\n        SqlInstance = $SqlInstance\n        Database    = $DatabaseFilter\n    }\n    if (-not $IncludeSystem) { $params['ExcludeSystem'] = $true }\n    if ($ExcludeDatabase) { $params['ExcludeDatabase'] = $ExcludeDatabase }\n\n    $databases = Get-DbaDatabase @params\n\n    foreach ($db in $databases) {\n        if ($PSCmdlet.ShouldProcess($db.Name, 'Backup')) {\n            Write-Host \"Backing up: $($db.Name)\" -ForegroundColor Yellow\n            $backupParams = @{ SqlInstance = $SqlInstance; Database = $db.Name }\n            if ($CompressBackup) { $backupParams['CompressBackup'] = $true }\n            Backup-DbaDatabase @backupParams\n        }\n    }\n}\n\n# Usage examples\nInvoke-DbaAction -SqlInstance 'localhost' -DatabaseFilter '*Sales*' -WhatIf\nInvoke-DbaAction -SqlInstance 'localhost' -CompressBackup -Force"}
          ]
        }
      ]
    },
    {
      "id": "module10",
      "title": "Module 10: Grafana Monitoring Dashboard",
      "sections": [
        {
          "id": "m10s1",
          "title": "10.1 Setting Up Grafana",
          "brief": "<h4>Setting Up Grafana</h4><ul><li>Grafana — open-source visualization and monitoring platform</li><li>Install on Windows or Docker container</li><li>Connect to SQL Server as data source directly</li><li>Or use InfluxDB/Prometheus as metrics store</li><li>PowerShell collects metrics → stores → Grafana displays</li><li>Default port: http://localhost:3000</li></ul>",
          "description": "<p><strong>Grafana is a powerful open-source platform for monitoring and observability.</strong> Combined with PowerShell for data collection, it creates a professional SQL Server monitoring dashboard that rivals expensive commercial tools.</p>",
          "syntax": "# Install Grafana (Windows)\n# Download from grafana.com/grafana/download\n# Or use Chocolatey:\nchoco install grafana -y\n\n# Start Grafana service\nStart-Service grafana\n\n# Access at http://localhost:3000\n# Default credentials: admin / admin\n\n# Install InfluxDB for metrics storage\nchoco install influxdb -y",
          "examples": [
            {"name": "Install Grafana", "code": "# Install and configure Grafana for SQL monitoring\n\n# Option 1: Chocolatey install\nchoco install grafana -y\n\n# Option 2: Manual download and install\n$grafanaUrl = 'https://dl.grafana.com/oss/release/grafana-10.2.0.windows-amd64.msi'\n$installer = 'C:\\Temp\\grafana-setup.msi'\nInvoke-WebRequest -Uri $grafanaUrl -OutFile $installer\nStart-Process msiexec.exe -ArgumentList \"/i $installer /quiet\" -Wait\n\n# Start the service\nStart-Service grafana\nGet-Service grafana | Select-Object Name, Status\n\nWrite-Host \"`nGrafana running at: http://localhost:3000\" -ForegroundColor Green\nWrite-Host 'Default login: admin / admin' -ForegroundColor Yellow"},
            {"name": "SQL Data Source", "code": "# Configure SQL Server as Grafana data source via API\n$grafanaUrl = 'http://localhost:3000'\n$apiKey = 'your-grafana-api-key'  # Create in Grafana UI\n\n# Add SQL Server data source\n$dataSource = @{\n    name      = 'SQL-PROD-01'\n    type      = 'mssql'\n    url       = 'SQL-PROD-01:1433'\n    database  = 'master'\n    user      = 'grafana_reader'\n    secureJsonData = @{ password = 'GrafanaRead123!' }\n    access    = 'proxy'\n} | ConvertTo-Json\n\nInvoke-RestMethod -Uri \"$grafanaUrl/api/datasources\" `\n    -Method Post `\n    -Headers @{ Authorization = \"Bearer $apiKey\"; 'Content-Type' = 'application/json' } `\n    -Body $dataSource\n\nWrite-Host 'SQL Server data source added to Grafana!' -ForegroundColor Green"},
            {"name": "InfluxDB Setup", "code": "# Setup InfluxDB for time-series metrics storage\n\n# Install InfluxDB\nchoco install influxdb -y\nStart-Service influxdb\n\n# Create database for SQL metrics\n$influxUrl = 'http://localhost:8086'\nInvoke-RestMethod -Uri \"$influxUrl/query\" -Method Post `\n    -Body @{ q = 'CREATE DATABASE sqlmetrics' }\n\n# Test writing a metric\n$timestamp = [DateTimeOffset]::UtcNow.ToUnixTimeSeconds()\n$metric = \"sql_cpu,server=SQL-PROD-01 value=45.2 $($timestamp)000000000\"\n\nInvoke-RestMethod -Uri \"$influxUrl/write?db=sqlmetrics\" `\n    -Method Post -Body $metric\n\nWrite-Host 'InfluxDB configured for SQL metrics!' -ForegroundColor Green\nWrite-Host 'Metrics DB: sqlmetrics' -ForegroundColor Cyan"}
          ]
        },
        {
          "id": "m10s2",
          "title": "10.2 Collecting Metrics with PowerShell",
          "brief": "<h4>Collecting Metrics with PowerShell</h4><ul><li>Query SQL Server DMVs for performance metrics</li><li>CPU, memory, disk I/O, wait statistics</li><li>Format as InfluxDB line protocol or JSON</li><li>Write to InfluxDB via REST API</li><li>Schedule with Task Scheduler (every 60 seconds)</li><li>DBATools commands for quick metric collection</li></ul>",
          "description": "<p><strong>PowerShell is the perfect tool for collecting SQL Server metrics and feeding them to Grafana.</strong> By querying DMVs (Dynamic Management Views) and using DBATools commands, you can gather comprehensive performance data and write it to a time-series database.</p>",
          "syntax": "# Collect CPU usage\nGet-DbaCpuUsage -SqlInstance server1\n\n# Collect wait statistics\nGet-DbaWaitStatistic -SqlInstance server1 -Threshold 10\n\n# Write to InfluxDB\n$metric = \"sql_cpu,server=server1 value=$cpuPercent\"\nInvoke-RestMethod -Uri 'http://influx:8086/write?db=sqlmetrics' `\n    -Method Post -Body $metric",
          "examples": [
            {"name": "Collect SQL Metrics", "code": "# Collect comprehensive SQL Server metrics\n$sqlInstance = 'localhost'\n\n# CPU Usage\n$cpu = Get-DbaCpuUsage -SqlInstance $sqlInstance |\n    Select-Object -Last 1\nWrite-Host \"SQL CPU: $($cpu.SQLProcessPercent)%\" -ForegroundColor Cyan\n\n# Memory usage\n$memory = Get-DbaMemoryUsage -SqlInstance $sqlInstance\n$memory | Select-Object SqlInstance, MemoryInUse, MemoryAvailable |\n    Format-Table -AutoSize\n\n# Wait statistics (top 10)\nGet-DbaWaitStatistic -SqlInstance $sqlInstance -Threshold 10 |\n    Select-Object -First 10 WaitType, WaitSeconds, WaitCount |\n    Format-Table -AutoSize\n\n# Database sizes\nGet-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem |\n    Select-Object Name, @{N='SizeMB';E={[math]::Round($_.SizeMB,2)}} |\n    Format-Table -AutoSize"},
            {"name": "Write to InfluxDB", "code": "# Write SQL metrics to InfluxDB for Grafana\n$sqlInstance = 'localhost'\n$influxUrl = 'http://localhost:8086/write?db=sqlmetrics'\n\nfunction Write-SqlMetrics {\n    param([string]$SqlInstance)\n\n    $timestamp = [DateTimeOffset]::UtcNow.ToUnixTimeNanoseconds()\n    $serverTag = $SqlInstance -replace '\\\\', '_'\n\n    # Collect metrics\n    $cpu = (Get-DbaCpuUsage -SqlInstance $SqlInstance | Select-Object -Last 1).SQLProcessPercent\n\n    $dbSizes = Get-DbaDatabase -SqlInstance $SqlInstance -ExcludeSystem\n    $totalSizeMB = ($dbSizes | Measure-Object SizeMB -Sum).Sum\n\n    # Format as InfluxDB line protocol\n    $metrics = @(\n        \"sql_cpu,server=$serverTag value=$cpu $timestamp\"\n        \"sql_db_total_size,server=$serverTag value=$totalSizeMB $timestamp\"\n        \"sql_db_count,server=$serverTag value=$($dbSizes.Count) $timestamp\"\n    )\n\n    # Write to InfluxDB\n    $body = $metrics -join \"`n\"\n    Invoke-RestMethod -Uri $influxUrl -Method Post -Body $body\n\n    Write-Host \"Metrics written: CPU=$cpu%, DBs=$($dbSizes.Count), Size=${totalSizeMB}MB\"\n}\n\n# Run once (schedule with Task Scheduler for continuous)\nWrite-SqlMetrics -SqlInstance $sqlInstance"},
            {"name": "Scheduled Collector", "code": "# Complete metric collector script (schedule every 60s)\n$config = @{\n    SqlInstances = @('localhost')\n    InfluxUrl    = 'http://localhost:8086/write?db=sqlmetrics'\n    Interval     = 60\n}\n\nfunction Invoke-MetricCollection {\n    foreach ($server in $config.SqlInstances) {\n        try {\n            $ts = [DateTimeOffset]::UtcNow.ToUnixTimeNanoseconds()\n            $tag = $server -replace '\\\\', '_'\n\n            # CPU\n            $cpu = (Invoke-DbaQuery -SqlInstance $server -Query @\"\n                SELECT TOP 1 SQLProcessUtilization\n                FROM sys.dm_os_ring_buffers\n                WHERE ring_buffer_type = 'RING_BUFFER_SCHEDULER_MONITOR'\n                ORDER BY timestamp DESC\n\"@).SQLProcessUtilization\n\n            # Connections\n            $conns = (Invoke-DbaQuery -SqlInstance $server -Query @\"\n                SELECT COUNT(*) AS cnt FROM sys.dm_exec_connections\n\"@).cnt\n\n            # Batch requests/sec\n            $batch = (Invoke-DbaQuery -SqlInstance $server -Query @\"\n                SELECT cntr_value FROM sys.dm_os_performance_counters\n                WHERE counter_name = 'Batch Requests/sec'\n\"@).cntr_value\n\n            $metrics = @(\n                \"sql_cpu,server=$tag value=$cpu $ts\"\n                \"sql_connections,server=$tag value=$conns $ts\"\n                \"sql_batch_requests,server=$tag value=$batch $ts\"\n            ) -join \"`n\"\n\n            Invoke-RestMethod -Uri $config.InfluxUrl -Method Post -Body $metrics\n            Write-Host \"[$server] CPU:$cpu% Conns:$conns Batch:$batch\"\n        } catch {\n            Write-Warning \"Error collecting from $server : $_\"\n        }\n    }\n}\n\nInvoke-MetricCollection"}
          ]
        },
        {
          "id": "m10s3",
          "title": "10.3 Building Dashboard Panels",
          "brief": "<h4>Building Dashboard Panels</h4><ul><li>Grafana panels: Gauge, Graph, Table, Stat, Alert List</li><li>SQL queries directly in Grafana panel editor</li><li>Time-series graphs for CPU, memory, I/O trends</li><li>Gauge panels for current utilization</li><li>Table panels for database inventory</li><li>Alert rules for threshold notifications</li></ul>",
          "description": "<p><strong>Grafana dashboards transform raw SQL Server metrics into actionable visual insights.</strong> Each panel type serves a different purpose — gauges for current state, graphs for trends, tables for inventory, and alerts for proactive notification.</p>",
          "syntax": "# Grafana SQL queries for panels\n\n# CPU trend (time-series)\n# SELECT timestamp, cpu_percent FROM metrics WHERE $__timeFilter(timestamp)\n\n# Database sizes (table)\n# SELECT name, size_mb FROM sys.databases ORDER BY size_mb DESC\n\n# Active connections (stat)\n# SELECT COUNT(*) FROM sys.dm_exec_connections\n\n# Create dashboard via API\n$dashboard = @{ dashboard = @{ title = 'SQL Server Monitor' } }\nInvoke-RestMethod -Uri 'http://localhost:3000/api/dashboards/db' `\n    -Method Post -Body ($dashboard | ConvertTo-Json -Depth 10)",
          "examples": [
            {"name": "Dashboard Queries", "code": "# SQL queries for Grafana panels\n\n# Panel 1: CPU Usage (Time Series)\n$cpuQuery = @\"\nSELECT\n    DATEADD(ms, -1 * (ts_now - [timestamp]), GETDATE()) AS time,\n    SQLProcessUtilization AS cpu_sql,\n    SystemIdle AS cpu_idle,\n    100 - SystemIdle - SQLProcessUtilization AS cpu_other\nFROM (\n    SELECT timestamp, DATEDIFF(ms, 0, GETDATE()) AS ts_now,\n        record.value('(./Record/SchedulerMonitorEvent/SystemHealth/SystemIdle)[1]','int') AS SystemIdle,\n        record.value('(./Record/SchedulerMonitorEvent/SystemHealth/ProcessUtilization)[1]','int') AS SQLProcessUtilization\n    FROM (\n        SELECT timestamp, CONVERT(xml, record) AS record\n        FROM sys.dm_os_ring_buffers\n        WHERE ring_buffer_type = 'RING_BUFFER_SCHEDULER_MONITOR'\n    ) AS x\n) AS y\nORDER BY time\n\"@\n\nWrite-Host '=== Grafana Panel Queries ===' -ForegroundColor Cyan\nWrite-Host \"`nCPU Time Series:\" -ForegroundColor Yellow\nWrite-Host $cpuQuery"},
            {"name": "Create Dashboard API", "code": "# Create Grafana dashboard via API\n$grafanaUrl = 'http://localhost:3000'\n$apiKey = 'your-api-key'\n\n$dashboard = @{\n    dashboard = @{\n        title = 'SQL Server - Production'\n        tags  = @('sql-server', 'production')\n        panels = @(\n            @{\n                title    = 'CPU Usage'\n                type     = 'timeseries'\n                gridPos  = @{h=8; w=12; x=0; y=0}\n            },\n            @{\n                title    = 'Memory Usage'\n                type     = 'gauge'\n                gridPos  = @{h=8; w=6; x=12; y=0}\n            },\n            @{\n                title    = 'Active Connections'\n                type     = 'stat'\n                gridPos  = @{h=4; w=6; x=18; y=0}\n            },\n            @{\n                title    = 'Database Sizes'\n                type     = 'table'\n                gridPos  = @{h=8; w=12; x=0; y=8}\n            }\n        )\n    }\n    overwrite = $true\n} | ConvertTo-Json -Depth 10\n\nInvoke-RestMethod -Uri \"$grafanaUrl/api/dashboards/db\" `\n    -Method Post `\n    -Headers @{ Authorization = \"Bearer $apiKey\"; 'Content-Type' = 'application/json' } `\n    -Body $dashboard\n\nWrite-Host 'Dashboard created!' -ForegroundColor Green"},
            {"name": "Alert Rules", "code": "# Configure Grafana alerts for SQL Server\n\n# PowerShell script to check and alert (alternative to Grafana alerts)\nfunction Test-SqlAlertConditions {\n    param([string]$SqlInstance = 'localhost')\n\n    $alerts = @()\n\n    # Check CPU\n    $cpu = (Get-DbaCpuUsage -SqlInstance $SqlInstance | Select-Object -Last 1).SQLProcessPercent\n    if ($cpu -gt 90) {\n        $alerts += \"CRITICAL: CPU at $cpu%\"\n    } elseif ($cpu -gt 75) {\n        $alerts += \"WARNING: CPU at $cpu%\"\n    }\n\n    # Check disk space\n    Get-DbaDiskSpace -ComputerName $SqlInstance | Where-Object PercentFree -lt 15 | ForEach-Object {\n        $alerts += \"WARNING: Drive $($_.Name) only $($_.PercentFree)% free\"\n    }\n\n    # Check for blocking\n    $blocking = Get-DbaProcess -SqlInstance $SqlInstance | Where-Object BlockingSessionId -ne 0\n    if ($blocking) {\n        $alerts += \"ALERT: $($blocking.Count) blocked sessions detected\"\n    }\n\n    # Output alerts\n    if ($alerts) {\n        Write-Host '=== SQL SERVER ALERTS ===' -ForegroundColor Red\n        $alerts | ForEach-Object { Write-Host \"  $_\" -ForegroundColor Yellow }\n    } else {\n        Write-Host 'All clear - no alerts' -ForegroundColor Green\n    }\n}\n\nTest-SqlAlertConditions"}
          ]
        }
      ]
    },
    {
      "id": "module11",
      "title": "Module 11: Daily DBA Reporting",
      "sections": [
        {
          "id": "m11s1",
          "title": "11.1 HTML Reports with ConvertTo-HTML",
          "brief": "<h4>HTML Reports with ConvertTo-HTML</h4><ul><li>ConvertTo-Html — built-in PowerShell HTML report generator</li><li>-Head parameter for CSS styling</li><li>-Fragment for multi-section reports</li><li>-PreContent and -PostContent for headers/footers</li><li>Combine multiple data sources into one report</li><li>Professional formatting with embedded CSS</li></ul>",
          "description": "<p><strong>ConvertTo-Html transforms PowerShell objects into formatted HTML tables.</strong> Combined with CSS styling, you can generate professional DBA reports that are easy to read and can be emailed or published to a web server.</p>",
          "syntax": "# Basic HTML report\nGet-DbaDatabase -SqlInstance server1 | ConvertTo-Html | Out-File report.html\n\n# Styled report\n$css = '<style>table{border-collapse:collapse}td,th{border:1px solid #ddd;padding:8px}</style>'\nGet-DbaDatabase -SqlInstance server1 |\n    Select-Object Name, Status, SizeMB |\n    ConvertTo-Html -Head $css -Title 'DB Report' |\n    Out-File report.html",
          "examples": [
            {"name": "Basic HTML Report", "code": "# Generate a styled HTML database report\n$sqlInstance = 'localhost'\n\n$css = @\"\n<style>\n    body { font-family: Segoe UI, sans-serif; margin: 20px; }\n    h1 { color: #2c3e50; }\n    table { border-collapse: collapse; width: 100%; margin-top: 15px; }\n    th { background-color: #533483; color: white; padding: 10px; text-align: left; }\n    td { border: 1px solid #ddd; padding: 8px; }\n    tr:nth-child(even) { background-color: #f2f2f2; }\n    .timestamp { color: #666; font-size: 12px; }\n</style>\n\"@\n\n$report = Get-DbaDatabase -SqlInstance $sqlInstance |\n    Select-Object Name, Status, RecoveryModel,\n        @{N='SizeMB';E={[math]::Round($_.SizeMB,2)}},\n        Owner, CreateDate |\n    ConvertTo-Html -Head $css `\n        -Title 'SQL Server Database Report' `\n        -PreContent \"<h1>Database Report - $sqlInstance</h1><p class='timestamp'>Generated: $(Get-Date)</p>\"\n\n$report | Out-File 'C:\\Reports\\DatabaseReport.html'\nWrite-Host 'Report saved to C:\\Reports\\DatabaseReport.html' -ForegroundColor Green"},
            {"name": "Multi-Section Report", "code": "# Multi-section DBA report using fragments\n$sqlInstance = 'localhost'\n\n$css = @\"\n<style>\n    body { font-family: Segoe UI; margin: 20px; background: #f5f5f5; }\n    h1 { color: #533483; } h2 { color: #e94560; margin-top: 30px; }\n    table { border-collapse: collapse; width: 100%; margin: 10px 0; }\n    th { background: #0f3460; color: white; padding: 8px; }\n    td { border: 1px solid #ddd; padding: 6px; }\n    tr:nth-child(even) { background: #e8e8e8; }\n</style>\n\"@\n\n# Section 1: Database Overview\n$dbSection = Get-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem |\n    Select-Object Name, Status, RecoveryModel, @{N='SizeMB';E={[math]::Round($_.SizeMB,2)}} |\n    ConvertTo-Html -Fragment -PreContent '<h2>Database Overview</h2>'\n\n# Section 2: Backup Status\n$backupSection = Get-DbaLastBackup -SqlInstance $sqlInstance |\n    Select-Object Database, LastFullBackup, LastDiffBackup, LastLogBackup |\n    ConvertTo-Html -Fragment -PreContent '<h2>Backup Status</h2>'\n\n# Section 3: Disk Space\n$diskSection = Get-DbaDiskSpace -ComputerName $sqlInstance |\n    Select-Object Name, Label, @{N='FreeGB';E={[math]::Round($_.Free/1GB,2)}}, PercentFree |\n    ConvertTo-Html -Fragment -PreContent '<h2>Disk Space</h2>'\n\n# Combine all sections\n$fullReport = ConvertTo-Html -Head $css -Body \"$dbSection $backupSection $diskSection\" `\n    -Title 'Daily DBA Report' `\n    -PreContent \"<h1>Daily DBA Report - $(Get-Date -Format 'yyyy-MM-dd')</h1>\"\n\n$fullReport | Out-File 'C:\\Reports\\DailyDBA.html'\nWrite-Host 'Multi-section report generated!' -ForegroundColor Green"},
            {"name": "Conditional Formatting", "code": "# HTML report with conditional formatting (color-coded)\n$sqlInstance = 'localhost'\n\n$css = @\"\n<style>\n    body { font-family: Segoe UI; margin: 20px; }\n    table { border-collapse: collapse; width: 100%; }\n    th { background: #533483; color: white; padding: 8px; }\n    td { border: 1px solid #ddd; padding: 6px; }\n    .ok { background-color: #d4edda; }\n    .warning { background-color: #fff3cd; }\n    .critical { background-color: #f8d7da; }\n</style>\n\"@\n\n# Build HTML with conditional colors\n$html = \"<html><head>$css</head><body>\"\n$html += \"<h1>Backup Health Report</h1>\"\n$html += \"<table><tr><th>Database</th><th>Last Backup</th><th>Age (Hours)</th><th>Status</th></tr>\"\n\nGet-DbaLastBackup -SqlInstance $sqlInstance | ForEach-Object {\n    $age = ((Get-Date) - $_.LastFullBackup).TotalHours\n    $class = switch ($age) {\n        {$_ -lt 24}  { 'ok' }\n        {$_ -lt 48}  { 'warning' }\n        default      { 'critical' }\n    }\n    $status = switch ($class) { 'ok'{'OK'} 'warning'{'WARNING'} 'critical'{'CRITICAL'} }\n    $html += \"<tr class='$class'><td>$($_.Database)</td><td>$($_.LastFullBackup)</td><td>$([math]::Round($age,1))</td><td>$status</td></tr>\"\n}\n\n$html += '</table></body></html>'\n$html | Out-File 'C:\\Reports\\BackupHealth.html'\nWrite-Host 'Color-coded report generated!' -ForegroundColor Green"}
          ]
        },
        {
          "id": "m11s2",
          "title": "11.2 Sending Email Reports",
          "brief": "<h4>Sending Email Reports</h4><ul><li>Send-MailMessage — built-in email cmdlet</li><li>-BodyAsHtml for HTML-formatted email body</li><li>-Attachments for report file attachments</li><li>Configure SMTP server and credentials</li><li>Schedule with Task Scheduler for daily delivery</li><li>Distribution lists for team notifications</li></ul>",
          "description": "<p><strong>Automated email reports keep your team informed without manual effort.</strong> Combine HTML report generation with Send-MailMessage to deliver daily DBA reports, backup status, and alerts directly to inboxes.</p>",
          "syntax": "# Send HTML email\nSend-MailMessage -From 'dba@company.com' -To 'team@company.com' `\n    -Subject 'Daily DBA Report' -Body $htmlContent -BodyAsHtml `\n    -SmtpServer 'smtp.company.com'\n\n# With attachment\nSend-MailMessage -From 'dba@company.com' -To 'team@company.com' `\n    -Subject 'Report' -Body 'See attached' `\n    -Attachments 'C:\\Reports\\report.html' `\n    -SmtpServer 'smtp.company.com'",
          "examples": [
            {"name": "Email Report", "code": "# Send DBA report via email\n$smtpServer = 'smtp.company.com'\n$from = 'dba-automation@company.com'\n$to = @('dba-team@company.com', 'manager@company.com')\n\n# Generate report content\n$sqlInstance = 'localhost'\n$reportHtml = Get-DbaDatabase -SqlInstance $sqlInstance -ExcludeSystem |\n    Select-Object Name, Status, RecoveryModel, @{N='SizeMB';E={[math]::Round($_.SizeMB,2)}} |\n    ConvertTo-Html -Fragment\n\n$body = @\"\n<html><body style='font-family:Segoe UI'>\n<h2>Daily Database Report - $(Get-Date -Format 'yyyy-MM-dd')</h2>\n<p>Server: $sqlInstance</p>\n$reportHtml\n<p style='color:gray;font-size:11px'>Generated automatically by PowerShell DBA Automation</p>\n</body></html>\n\"@\n\n# Send email\nSend-MailMessage -From $from -To $to `\n    -Subject \"DBA Report - $(Get-Date -Format 'yyyy-MM-dd')\" `\n    -Body $body -BodyAsHtml `\n    -SmtpServer $smtpServer -Port 25\n\nWrite-Host 'Report emailed successfully!' -ForegroundColor Green"},
            {"name": "Alert Email", "code": "# Send alert emails for critical conditions\nfunction Send-DbaAlert {\n    param(\n        [string]$SqlInstance,\n        [string]$Subject,\n        [string]$Body,\n        [ValidateSet('Info','Warning','Critical')]\n        [string]$Severity = 'Warning'\n    )\n\n    $colors = @{ Info='#17a2b8'; Warning='#ffc107'; Critical='#dc3545' }\n    $color = $colors[$Severity]\n\n    $htmlBody = @\"\n<html><body style='font-family:Segoe UI'>\n<div style='border-left:4px solid $color;padding:10px;margin:10px 0'>\n<h3 style='color:$color'>[$Severity] $Subject</h3>\n<p>Server: $SqlInstance</p>\n<p>Time: $(Get-Date)</p>\n<p>$Body</p>\n</div>\n</body></html>\n\"@\n\n    Send-MailMessage -From 'dba-alerts@company.com' `\n        -To 'dba-oncall@company.com' `\n        -Subject \"[$Severity] $Subject - $SqlInstance\" `\n        -Body $htmlBody -BodyAsHtml `\n        -SmtpServer 'smtp.company.com' `\n        -Priority $(if($Severity -eq 'Critical'){'High'}else{'Normal'})\n}\n\n# Usage\nSend-DbaAlert -SqlInstance 'SQL-PROD-01' `\n    -Subject 'Disk Space Low' `\n    -Body 'Drive E: is at 92% capacity. Immediate action required.' `\n    -Severity Critical"},
            {"name": "Scheduled Report", "code": "# Complete scheduled report script\n# Save as C:\\Scripts\\DailyDBAReport.ps1\n# Schedule with Task Scheduler to run daily at 7:00 AM\n\n$config = @{\n    SqlInstances = @('localhost')\n    SmtpServer   = 'smtp.company.com'\n    From         = 'dba-reports@company.com'\n    To           = @('dba-team@company.com')\n    ReportPath   = 'C:\\Reports'\n}\n\n# Collect data\n$allData = foreach ($server in $config.SqlInstances) {\n    Get-DbaLastBackup -SqlInstance $server | Select-Object SqlInstance, Database,\n        LastFullBackup, @{N='HoursAgo';E={[math]::Round(((Get-Date)-$_.LastFullBackup).TotalHours,1)}}\n}\n\n# Build report\n$staleBackups = $allData | Where-Object HoursAgo -gt 24\n$subject = if ($staleBackups) {\n    \"[WARNING] DBA Report - $($staleBackups.Count) stale backups\"\n} else {\n    \"[OK] DBA Report - All backups current\"\n}\n\n$body = $allData | ConvertTo-Html -Fragment |\n    Out-String\n\nSend-MailMessage -From $config.From -To $config.To `\n    -Subject $subject -Body $body -BodyAsHtml `\n    -SmtpServer $config.SmtpServer\n\nWrite-Host \"Report sent: $subject\" -ForegroundColor Green"}
          ]
        },
        {
          "id": "m11s3",
          "title": "11.3 Backup & DBCC Reports",
          "brief": "<h4>Backup & DBCC Reports</h4><ul><li>Get-DbaLastBackup — backup status for all databases</li><li>Get-DbaLastGoodCheckDb — last successful DBCC CHECKDB</li><li>Combine into a single health report</li><li>Flag databases with stale backups or missing DBCC</li><li>Include disk space and growth trends</li><li>Automated daily delivery to DBA team</li></ul>",
          "description": "<p><strong>Backup and DBCC CHECKDB reports are the two most important daily checks for any DBA.</strong> Stale backups mean data loss risk, and missing DBCC checks mean undetected corruption. Automate these reports to catch issues before they become disasters.</p>",
          "syntax": "# Backup status\nGet-DbaLastBackup -SqlInstance server1 |\n    Select-Object Database, LastFullBackup, LastDiffBackup, LastLogBackup\n\n# DBCC CHECKDB status\nGet-DbaLastGoodCheckDb -SqlInstance server1 |\n    Select-Object Database, LastGoodCheckDb, DaysSinceLastGoodCheckDb\n\n# Database space\nGet-DbaDbSpace -SqlInstance server1 |\n    Select-Object Database, FileName, UsedSpace, FreeSpace",
          "examples": [
            {"name": "Backup Report", "code": "# Comprehensive backup status report\n$servers = 'localhost'\n\n$backupReport = Get-DbaLastBackup -SqlInstance $servers |\n    Select-Object SqlInstance, Database,\n        @{N='LastFull';E={$_.LastFullBackup}},\n        @{N='FullAge_Hrs';E={[math]::Round(((Get-Date)-$_.LastFullBackup).TotalHours,1)}},\n        @{N='LastLog';E={$_.LastLogBackup}},\n        @{N='LogAge_Min';E={[math]::Round(((Get-Date)-$_.LastLogBackup).TotalMinutes,0)}},\n        @{N='Status';E={\n            if (((Get-Date)-$_.LastFullBackup).TotalHours -gt 48) {'CRITICAL'}\n            elseif (((Get-Date)-$_.LastFullBackup).TotalHours -gt 24) {'WARNING'}\n            else {'OK'}\n        }}\n\nWrite-Host '=== Backup Status Report ===' -ForegroundColor Cyan\n$backupReport | Format-Table -AutoSize\n\n# Summary\n$critical = ($backupReport | Where-Object Status -eq 'CRITICAL').Count\n$warning = ($backupReport | Where-Object Status -eq 'WARNING').Count\nWrite-Host \"`nSummary: $critical CRITICAL, $warning WARNING\" -ForegroundColor $(if($critical){'Red'}else{'Green'})"},
            {"name": "DBCC Report", "code": "# DBCC CHECKDB status report\n$servers = 'localhost'\n\n$dbccReport = Get-DbaLastGoodCheckDb -SqlInstance $servers |\n    Select-Object SqlInstance, Database,\n        LastGoodCheckDb,\n        @{N='DaysSince';E={$_.DaysSinceLastGoodCheckDb}},\n        @{N='Status';E={\n            if ($_.DaysSinceLastGoodCheckDb -gt 14) {'CRITICAL'}\n            elseif ($_.DaysSinceLastGoodCheckDb -gt 7) {'WARNING'}\n            else {'OK'}\n        }}\n\nWrite-Host '=== DBCC CHECKDB Report ===' -ForegroundColor Cyan\n$dbccReport | Format-Table -AutoSize\n\n# Databases needing CHECKDB\n$needsCheck = $dbccReport | Where-Object DaysSince -gt 7\nif ($needsCheck) {\n    Write-Host \"`nDatabases needing DBCC CHECKDB:\" -ForegroundColor Yellow\n    $needsCheck | Select-Object Database, DaysSince | Format-Table\n}"},
            {"name": "Combined Health Report", "code": "# Combined daily health report\n$sqlInstance = 'localhost'\n$reportDate = Get-Date -Format 'yyyy-MM-dd'\n\nWrite-Host \"=== Daily DBA Health Report - $reportDate ===\" -ForegroundColor Cyan\n\n# 1. Backup Status\nWrite-Host \"`n[BACKUPS]\" -ForegroundColor Yellow\n$backups = Get-DbaLastBackup -SqlInstance $sqlInstance\n$stale = $backups | Where-Object { ((Get-Date) - $_.LastFullBackup).TotalHours -gt 24 }\nWrite-Host \"  Total databases: $($backups.Count)\"\nWrite-Host \"  Stale backups (>24h): $($stale.Count)\" -ForegroundColor $(if($stale){'Red'}else{'Green'})\n\n# 2. DBCC Status\nWrite-Host \"`n[DBCC CHECKDB]\" -ForegroundColor Yellow\n$dbcc = Get-DbaLastGoodCheckDb -SqlInstance $sqlInstance\n$oldCheck = $dbcc | Where-Object DaysSinceLastGoodCheckDb -gt 7\nWrite-Host \"  Checked within 7 days: $($dbcc.Count - $oldCheck.Count)\"\nWrite-Host \"  Overdue: $($oldCheck.Count)\" -ForegroundColor $(if($oldCheck){'Red'}else{'Green'})\n\n# 3. Disk Space\nWrite-Host \"`n[DISK SPACE]\" -ForegroundColor Yellow\nGet-DbaDiskSpace -ComputerName $sqlInstance |\n    Select-Object Name, @{N='FreeGB';E={[math]::Round($_.Free/1GB,1)}}, PercentFree |\n    ForEach-Object {\n        $color = if ($_.PercentFree -lt 15) {'Red'} elseif ($_.PercentFree -lt 25) {'Yellow'} else {'Green'}\n        Write-Host \"  $($_.Name): $($_.FreeGB)GB free ($($_.PercentFree)%)\" -ForegroundColor $color\n    }\n\nWrite-Host \"`n=== Report Complete ===\" -ForegroundColor Cyan"}
          ]
        }
      ]
    },
    {
      "id": "module12",
      "title": "Module 12: Database Migration",
      "sections": [
        {
          "id": "m12s1",
          "title": "12.1 Migration Planning with DBATools",
          "brief": "<h4>Migration Planning with DBATools</h4><ul><li>Start-DbaMigration — one command to migrate everything</li><li>Test-DbaMigrationConstraint — pre-migration checks</li><li>Copy-DbaDatabase — database-level migration</li><li>Assessment: compatibility, features, deprecated syntax</li><li>Plan: logins, jobs, linked servers, mail, credentials</li><li>Validate: compare source and destination post-migration</li></ul>",
          "description": "<p><strong>Database migration is one of the most complex DBA tasks.</strong> DBATools simplifies this with Start-DbaMigration, which can migrate an entire SQL Server instance — databases, logins, jobs, linked servers, and more — in a single command.</p>",
          "syntax": "# Pre-migration assessment\nTest-DbaMigrationConstraint -Source server1 -Destination server2\n\n# Full instance migration\nStart-DbaMigration -Source server1 -Destination server2 -BackupRestore `\n    -SharedPath '\\\\share\\migration'\n\n# Selective migration\nCopy-DbaDatabase -Source server1 -Destination server2 -Database 'SalesDB'\nCopy-DbaLogin -Source server1 -Destination server2\nCopy-DbaAgentJob -Source server1 -Destination server2",
          "examples": [
            {"name": "Pre-Migration Check", "code": "# Pre-migration assessment\n$source = 'SQL-OLD-01'\n$destination = 'SQL-NEW-01'\n\n# Test migration constraints\nWrite-Host '=== Migration Assessment ===' -ForegroundColor Cyan\n\n# Check compatibility\nTest-DbaMigrationConstraint -Source $source -Destination $destination |\n    Select-Object SourceInstance, DestinationInstance, Name, IsMigratable, Notes |\n    Format-Table -AutoSize\n\n# Compare versions\nWrite-Host \"`nVersion Comparison:\" -ForegroundColor Yellow\n$srcBuild = Get-DbaBuild -SqlInstance $source\n$dstBuild = Get-DbaBuild -SqlInstance $destination\nWrite-Host \"  Source:      $($srcBuild.Build) ($($srcBuild.NameLevel) $($srcBuild.SPLevel) $($srcBuild.CULevel))\"\nWrite-Host \"  Destination: $($dstBuild.Build) ($($dstBuild.NameLevel) $($dstBuild.SPLevel) $($dstBuild.CULevel))\"\n\n# Check deprecated features in use\nWrite-Host \"`nDeprecated Features:\" -ForegroundColor Yellow\nGet-DbaDeprecatedFeature -SqlInstance $source |\n    Select-Object Feature, InstanceCount | Format-Table -AutoSize"},
            {"name": "Migration Inventory", "code": "# Create migration inventory/checklist\n$source = 'SQL-OLD-01'\n\nWrite-Host '=== Migration Inventory ===' -ForegroundColor Cyan\n\n# Databases\n$dbs = Get-DbaDatabase -SqlInstance $source -ExcludeSystem\nWrite-Host \"`nDatabases: $($dbs.Count)\" -ForegroundColor Yellow\n$dbs | Select-Object Name, @{N='SizeMB';E={[math]::Round($_.SizeMB,2)}}, RecoveryModel |\n    Format-Table -AutoSize\n\n# Logins\n$logins = Get-DbaLogin -SqlInstance $source | Where-Object { -not $_.IsSystemObject }\nWrite-Host \"Logins: $($logins.Count)\" -ForegroundColor Yellow\n\n# Agent Jobs\n$jobs = Get-DbaAgentJob -SqlInstance $source\nWrite-Host \"Agent Jobs: $($jobs.Count)\" -ForegroundColor Yellow\n\n# Linked Servers\n$linked = Get-DbaLinkedServer -SqlInstance $source\nWrite-Host \"Linked Servers: $($linked.Count)\" -ForegroundColor Yellow\n\n# Total size\n$totalGB = [math]::Round(($dbs | Measure-Object SizeMB -Sum).Sum / 1024, 2)\nWrite-Host \"`nTotal Database Size: $totalGB GB\" -ForegroundColor Cyan\nWrite-Host \"Estimated Migration Time: $([math]::Round($totalGB / 10, 0)) minutes (at ~10GB/min)\" -ForegroundColor Cyan"},
            {"name": "Migration Script", "code": "# Complete migration script with validation\n$source = 'SQL-OLD-01'\n$destination = 'SQL-NEW-01'\n$sharePath = '\\\\fileserver\\migration'\n\n$startTime = Get-Date\nWrite-Host \"Migration started: $startTime\" -ForegroundColor Cyan\n\n# Step 1: Copy Logins (do first to avoid orphans)\nWrite-Host \"`n[1/5] Copying logins...\" -ForegroundColor Yellow\nCopy-DbaLogin -Source $source -Destination $destination -Force\n\n# Step 2: Copy Agent Jobs\nWrite-Host \"[2/5] Copying Agent jobs...\" -ForegroundColor Yellow\nCopy-DbaAgentJob -Source $source -Destination $destination -Force\n\n# Step 3: Copy Linked Servers\nWrite-Host \"[3/5] Copying linked servers...\" -ForegroundColor Yellow\nCopy-DbaLinkedServer -Source $source -Destination $destination -Force\n\n# Step 4: Migrate Databases\nWrite-Host \"[4/5] Migrating databases...\" -ForegroundColor Yellow\nCopy-DbaDatabase -Source $source -Destination $destination `\n    -BackupRestore -SharedPath $sharePath -Force\n\n# Step 5: Validate\nWrite-Host \"[5/5] Validating...\" -ForegroundColor Yellow\n$srcDbs = (Get-DbaDatabase -SqlInstance $source -ExcludeSystem).Count\n$dstDbs = (Get-DbaDatabase -SqlInstance $destination -ExcludeSystem).Count\n\n$duration = (Get-Date) - $startTime\nWrite-Host \"`n=== Migration Complete ===\" -ForegroundColor Green\nWrite-Host \"Duration: $($duration.ToString('hh\\:mm\\:ss'))\"\nWrite-Host \"Databases: Source=$srcDbs, Destination=$dstDbs\""}
          ]
        },
        {
          "id": "m12s2",
          "title": "12.2 Instance & Database Migration",
          "brief": "<h4>Instance & Database Migration</h4><ul><li>Start-DbaMigration — the ultimate migration command</li><li>Migrates: databases, logins, jobs, linked servers, credentials, mail</li><li>-BackupRestore method for cross-network migrations</li><li>-SharedPath for backup file staging</li><li>Post-migration validation and comparison</li><li>Rollback planning and cutover procedures</li></ul>",
          "description": "<p><strong>Start-DbaMigration is DBATools' flagship command — it migrates an entire SQL Server instance in one operation.</strong> It handles databases, logins, Agent jobs, linked servers, credentials, database mail, and more. This is the command that makes DBATools famous.</p>",
          "syntax": "# Full instance migration (everything)\nStart-DbaMigration -Source server1 -Destination server2 `\n    -BackupRestore -SharedPath '\\\\share\\migration'\n\n# Selective migration\nStart-DbaMigration -Source server1 -Destination server2 `\n    -BackupRestore -SharedPath '\\\\share\\migration' `\n    -Exclude Databases  # Migrate everything EXCEPT databases\n\n# Database-only migration\nCopy-DbaDatabase -Source server1 -Destination server2 `\n    -Database 'Sales','Inventory' -BackupRestore -SharedPath '\\\\share\\temp'",
          "examples": [
            {"name": "Full Migration", "code": "# Full instance migration with Start-DbaMigration\n$source = 'SQL-2014-PROD'\n$destination = 'SQL-2022-PROD'\n$sharePath = '\\\\migration-share\\staging'\n\n# This single command migrates EVERYTHING\nStart-DbaMigration -Source $source -Destination $destination `\n    -BackupRestore `\n    -SharedPath $sharePath `\n    -Force `\n    -Verbose\n\n# What gets migrated:\n# - All user databases (backup/restore)\n# - All logins (with SIDs and passwords)\n# - SQL Agent jobs and schedules\n# - Linked servers\n# - Credentials\n# - Database mail configuration\n# - Server triggers\n# - Custom error messages\n# - Startup procedures\n\nWrite-Host \"`nFull instance migration complete!\" -ForegroundColor Green"},
            {"name": "Selective Migration", "code": "# Migrate specific components\n$source = 'SQL-OLD-01'\n$destination = 'SQL-NEW-01'\n$sharePath = '\\\\fileserver\\migration'\n\n# Migrate only specific databases\n$databasesToMigrate = 'Sales', 'Inventory', 'CRM', 'HR'\n\nCopy-DbaDatabase -Source $source -Destination $destination `\n    -Database $databasesToMigrate `\n    -BackupRestore -SharedPath $sharePath -Force\n\n# Migrate logins for those databases\n$dbLogins = foreach ($db in $databasesToMigrate) {\n    Get-DbaDbUser -SqlInstance $source -Database $db |\n        Where-Object Login -ne '' | Select-Object -ExpandProperty Login\n}\nCopy-DbaLogin -Source $source -Destination $destination `\n    -Login ($dbLogins | Select-Object -Unique)\n\n# Migrate related Agent jobs\nGet-DbaAgentJob -SqlInstance $source |\n    Where-Object { $_.JobSteps.DatabaseName -in $databasesToMigrate } |\n    Copy-DbaAgentJob -Destination $destination -Force\n\nWrite-Host 'Selective migration complete!' -ForegroundColor Green"},
            {"name": "Post-Migration Validation", "code": "# Validate migration was successful\n$source = 'SQL-OLD-01'\n$destination = 'SQL-NEW-01'\n\nWrite-Host '=== Post-Migration Validation ===' -ForegroundColor Cyan\n\n# Compare database counts\n$srcDbs = Get-DbaDatabase -SqlInstance $source -ExcludeSystem\n$dstDbs = Get-DbaDatabase -SqlInstance $destination -ExcludeSystem\nWrite-Host \"`n[Databases] Source: $($srcDbs.Count) | Destination: $($dstDbs.Count)\" `\n    -ForegroundColor $(if($srcDbs.Count -eq $dstDbs.Count){'Green'}else{'Red'})\n\n# Compare logins\n$srcLogins = (Get-DbaLogin -SqlInstance $source | Where-Object {-not $_.IsSystemObject}).Count\n$dstLogins = (Get-DbaLogin -SqlInstance $destination | Where-Object {-not $_.IsSystemObject}).Count\nWrite-Host \"[Logins] Source: $srcLogins | Destination: $dstLogins\" `\n    -ForegroundColor $(if($srcLogins -eq $dstLogins){'Green'}else{'Yellow'})\n\n# Compare Agent jobs\n$srcJobs = (Get-DbaAgentJob -SqlInstance $source).Count\n$dstJobs = (Get-DbaAgentJob -SqlInstance $destination).Count\nWrite-Host \"[Agent Jobs] Source: $srcJobs | Destination: $dstJobs\" `\n    -ForegroundColor $(if($srcJobs -eq $dstJobs){'Green'}else{'Yellow'})\n\n# Test all connections on destination\nWrite-Host \"`n[Connection Test]\" -ForegroundColor Yellow\nTest-DbaConnection -SqlInstance $destination |\n    Select-Object SqlInstance, ConnectSuccess, AuthType |\n    Format-Table -AutoSize\n\n# Check for orphaned users\n$orphans = Get-DbaDbOrphanUser -SqlInstance $destination\nWrite-Host \"[Orphaned Users] $($orphans.Count) found\" `\n    -ForegroundColor $(if($orphans.Count -eq 0){'Green'}else{'Red'})\n\nWrite-Host \"`n=== Validation Complete ===\" -ForegroundColor Cyan"}
          ]
        }
      ]
    }
  ]
};
